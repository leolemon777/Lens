# 屏迹 ScreenTrace 隐私说明

更新日期：2026-08-10

本文档描述当前仓库中 macOS 开发版的实际行为。屏迹尚未公开发布；未来如果增加可选云分享、在线模型或更新服务，必须在发布前更新本说明和应用内授权界面。

## 默认本地处理

当前开发版不包含帐号、分析埋点、广告 SDK、云上传或自动更新网络客户端。截图、录屏、OCR、转写、自动整理、降噪和导出都在本机完成。

屏迹只在用户主动开始捕获或打开相关功能时请求系统权限；拒绝权限不会导致已保存原始素材被删除。

## 可能处理的数据

- 屏幕与系统音频：用于用户主动发起的截图、长截图或录屏。
- 麦克风与摄像头：仅在录制菜单中显式开启后写入独立轨道。
- 指针、点击和键盘事件：用于自动运镜和点击效果。键盘轨只保留 Command/Control 快捷键与非文本控制键；普通文字、密码和仅 Option 输入不会被记录。
- 前台应用事件：可保留应用名称和 Bundle ID，不保留窗口标题或文档名。
- 捕获来源元数据：项目可保留显示器/窗口 ID、选区、应用名称和当时的窗口标题，便于恢复来源与重录。窗口标题可能含有敏感内容，分享 `.screentrace` 项目包前应先检查 Manifest。
- OCR、转写与整理结果：保存在项目包的 `analysis/` 目录中。敏感信息检测只把掩码后的提示写入整理文件，不复制命中的原值。

## 本地存储

- 截图、录屏、事件轨、分析和编辑计划默认保存在 `~/Pictures/ScreenTrace/` 下的 `.screentrace` 开放项目包。
- 可丢弃的屏迹库索引保存在 `~/Pictures/ScreenTrace/.index/`，损坏或删除后可从项目包重建。
- 本地诊断事件与会话健康标记保存在 `~/Library/Application Support/ScreenTrace/diagnostics/`。事件日志为 512 KiB 双文件轮转。
- macOS 可能在 `~/Library/Logs/DiagnosticReports/` 中生成崩溃报告。屏迹只在本机读取与本 App 匹配的有限指纹字段，不自动上传报告。

## 诊断数据

诊断日志仅允许固定事件码和白名单元数据，错误只保留 domain/code。诊断摘要不包含截图、录屏、声音、OCR/转写正文、调用栈、窗口标题或项目路径。只有用户主动点击“复制诊断摘要”并自行发送时，摘要才会离开本机。

## 权限用途

- 屏幕与系统音频录制：截图和录屏。
- 麦克风：可选讲解声轨。
- 摄像头：可选人像轨。
- 语音识别：Apple 设备端转写；不支持本地识别时不会自动回退到云端。
- 辅助功能：当前权限中心会显示状态；未授权不影响本地项目的读取和导出。

## 删除和导出

用户可直接复制、移动、备份或删除 `.screentrace` 项目包。完全清除当前开发版的本地数据时，退出屏迹后删除 `~/Pictures/ScreenTrace/`、`~/Library/Application Support/ScreenTrace/` 和 `~/Library/Preferences/app.screentrace.mac.plist`；如需清除 macOS 自身的崩溃报告，还需单独检查 `~/Library/Logs/DiagnosticReports/`。屏幕录制、麦克风、摄像头、语音识别与辅助功能授权由 macOS 管理，需在“系统设置 → 隐私与安全性”中单独撤回。


## 分享前检查

PNG、JPEG 和 MP4 导出只包含用户选择的成片内容；`.screentrace` 项目包还可能包含原始媒体、独立麦克风/摄像头轨、事件轨、OCR、转写、标题和编辑计划。向他人分享整个项目包前，应将它视为原始工作文件并逐项检查。
