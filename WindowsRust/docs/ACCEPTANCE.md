# Windows 真机验收表

状态：**本次全部未运行**。仅编译成功或浏览器模拟测试通过，不得把此表自动改成通过。

记录：Windows 版本 / CPU / GPU 与驱动 / 显示器分辨率与缩放 / 输入输出音频设备 / EXE SHA-256 / Cargo.lock SHA-256。

| 项目 | 操作与通过条件 | 状态 |
|---|---|---|
| 构建 | Rust core tests、Windows 编译均退出 0；产物为真实 PE EXE | 未运行 |
| 启动 | 无开发工具的目标机启动；WebView2 已安装；不要求管理员权限 | 未运行 |
| PNG | 全屏/窗口/区域各一次；方向正确、边界正确、颜色合理 | 未运行 |
| 基础视频 | 1080p/30fps 录10秒，文字方向正确、时长正确、画面可播 | 未运行 |
| 静止桌面 | 画面静止20秒再移动，视频包含完整20秒停留 | 未运行 |
| 窗口录制 | 只录目标窗口；关闭、最小化、改变尺寸时明确失败并保留素材 | 未运行 |
| 区域 | 边界标记不可出现在选区外；100%/125%/150%/200% 缩放验证 | 未运行 |
| 多显示器 | 主屏、副屏、负坐标、不同DPI、断开显示器 | 未运行 |
| 系统声 | 播放→静音10秒→播放，不得把第二段声音提前 | 未运行 |
| 麦克风 | 默认输入设备可录，缺失/拒绝权限清晰报错 | 未运行 |
| 双音轨 | 系统声和麦克风同时开启，独立 WAV 存在且成片均有声音 | 未运行 |
| 暂停 | 录5秒→暂停10秒→录5秒；输出约10秒，轨道同步 | 未运行 |
| 连续操作 | 重复点击开始/暂停/停止不产生并发会话或覆盖原文件 | 未运行 |
| 热键 | 切换到其他应用后F9/F10有效；冲突有明确提示 | 未运行 |
| 退出 | 录制、暂停、合成时关闭窗口被阻止；停止完成后可正常退出 | 未运行 |
| 长录制 | 30分钟→60分钟；测量音画偏差、内存、CPU/GPU、磁盘增长 | 未运行 |
| 低磁盘 | 使用独立测试卷，不消耗系统盘；触发阈值尝试停止、素材保留 | 未运行 |
| 强退 | 一段完成后开始第二段并强退；重启可重试第一段，不承诺第二段恢复 | 未运行 |
| 无组件 | 移走FFmpeg后声音/暂停不可选，无声单段与截图仍可走通 | 未运行 |
| 文件兼容 | 将项目副本交给Mac版读取，核对日期、时长、素材、角色、缺省字段 | 未运行 |
| 隐私 | 无网络上传，无普通键盘文本日志；路径不能跳出项目根目录 | 未运行 |

在上述基础项通过之前，不要用这个首版录制不可重来的重要内容。以单独的新工程继续迭代，不修改已经工作的 macOS 版本。
