# 截屏简化试用版

交付目录：`Build/Releases/Windows/UI-Review-02/Lens-0.1.0-x64/app`。

截图只有一个入口：点击“截图”直接拖动截屏，松手完成。截图菜单中的窗口截图、合成、当前屏幕及区域录制选项已移除。屏幕、窗口和区域的选择属于录屏设置。本轮没有启动或测试录屏。

修复了 Ctrl+Alt+1 错误打开操作中心的问题：快捷键使用实际按键匹配，避免显示字符串中的 Digit1 与“1”不一致。

Computer Use 截屏实测：

- 点击“截图”直接打开 Lens 选区，无模式菜单。
- 拖动桌面壁纸中的无隐私区域，松手后直接生成 Quick Access，没有再次点击确认。
- 实际保存的 PNG 为 570 × 225，已打开核对。素材库数量由 5 变为 6。
- 再次进入截屏后按 Esc，遮罩关闭，素材库数量保持 6，没有误保存。
- 最终包复测 Ctrl+Alt+1：直接打开 Lens 选区，随后 Esc 正常退出。

测试图片：`E:/Lens/20260911T060636Z-d07bab02.lens/raw/screenshot.png`。图片包含 Computer Use 的测试指针提示。

前端构建与现有透明度、产品权限回归检查通过；Release / NSIS 包独立生成。桌面 Lens 快捷方式用于指向本交付目录。
