# Lens macOS 发布检查清单

状态：Public Alpha/Beta 前必须逐项留存证据。本清单不把本地 ad-hoc 签名视为发布签名。

## 1. 发布范围与法务

- [x] 当前候选固定为 `0.1.0 (1)`、`beta`、最低 macOS 15.2；四项均进入 `release.json` 并由独立校验器反查。
- [ ] 选定开源许可证，在根目录添加 `LICENSE`，审查所有依赖与媒体资产许可。
- [x] `Assets/AppIcon.png` 已按 16/32/128/256 px 复核轮廓辨识度，固定摘要与来源记录一致，Release App 包含有效 `AppIcon.icns`。
- [x] `bash Scripts/audit-public-source.sh` 通过，`docs/第三方依赖与素材清单.md` 与当前无远程依赖、单一原创图标资产的构建一致。
- [x] `README.md`、`PRIVACY.md`、`SECURITY.md` 和 `CONTRIBUTING.md` 已按当前二进制、618 项 G0 基线和 Beta 候选行为复核。
- [ ] 配置私密安全报告通道与公开支持联系方式。
- [x] 公开源码审计拒绝真实媒体、崩溃报告、项目包、发布附件与签名/公证凭据；当前 Git 跟踪集未发现上述内容。

## 2. 代码与项目格式

- [ ] `git status --short` 为空，发布 commit 已标记。
- [x] `docs/Lens 开放项目格式-v0.1.md` 与 `LensProjectSchema.portableDocuments` 一致，所有可移植 JSON 文档的最低可读和当前写入版本均已记录。
- [x] `ProjectSchemaCompatibilityTests` 使用旧版录屏/截图黄金样本验证读取字节不变，并验证未来或畸形 schema 被拒绝且不会进入 Lens 库。
- [x] 损坏索引可重建，中断录制可发现，原始媒体和分片不被恢复流程删除。

## 3. 自动化门槛

- [x] `swift test` 最终源码全量 618 项通过、0 失败；已用当前源码重新生成 G0 报告。Public Beta 打包前仍需在干净的最终发布 commit 上再生成一次。
- [x] `swift build -c release --product Lens -Xswiftc -warnings-as-errors` 通过。
- [x] `bash Scripts/build-release-artifacts.sh` 通过 Info.plist、签名结构、dSYM UUID 一致性和 zip 完整性检查。
- [x] `bash Scripts/package-macos-release.sh` 生成 DMG、App zip、dSYM、机器可读发布清单、发布说明与 SHA-256 清单；DMG 重新挂载后 App 严格验签且 Applications 链接正确。最终候选由 `verify-macos-release.sh --development` 独立解包/挂载复验并完成不可覆盖归档；具体身份以最后生成的 `release.json` 为准。
- [ ] `bash Scripts/verify-macos-release.sh <release.json>` 公开模式通过，从 App zip、dSYM 与只读 DMG 独立反查摘要、元数据、UUID、图标、签名、公证、Staple 和 Gatekeeper。
  - [x] 开发模式现在允许诚实标记为 `dirty` 的开发快照，但会把全部已跟踪文件及未跟踪但未忽略文件计算为确定性源码快照 SHA-256，并核对摘要与当前工作区一致；公开模式继续强制最终 commit 与工作区均为 `clean`。
  - [x] 最新开发候选已通过独立开发验证；具体构建号和源码快照摘要以 `Build/Releases/` 中最后生成的 `release.json` 为准。公开模式反向验证按预期退出 65，并一次列出 dirty、LICENSE、Developer ID、Team ID、公证、Staple 与 Gatekeeper 共 14 个阻断项。
  - [x] 不可覆盖归档记录 Git commit 与源码快照摘要，复验历史档案时不错误依赖当前工作区仍处于旧源码；当前源码变化后旧归档仍通过，归档内单文件内容被篡改则 SHA-256 门按预期退出 65。
- [x] App/dSYM Mach-O UUID 与符号包 SHA-256 已进入发布档案；匹配 UUID 的合成 `.ips` 已准确还原 Swift 源文件和行号。
- [x] 真实 H.264、音频混合、摄像头画中画、转场、字幕、标注、PNG/JPEG、OCR 和长截图集成测试均包含在当前 618 项发布基线并通过。

## 4. 解锁 Mac 真机验收

- [ ] 固定签名身份后重新授予屏幕与系统音频录制、麦克风、摄像头、语音识别与辅助功能权限。
- [ ] 使用内置键盘、Magic Keyboard 和第三方键盘验证 `Fn + Control`、`Fn + Space`、自定义和无 Fn 备用快捷键。
- [ ] 在单屏、多屏、Retina/非 Retina 混合缩放与不同排列下完成区域、窗口、多窗口和当前显示器截图。
- [ ] 在 Safari、Chrome 和普通 App 中验证长截图、OCR、标注、背景编辑、贴图和 PNG/JPEG 导出。
- [x] 验证区域、窗口和显示器录制的系统声/麦克风/摄像头分轨、暂停/继续、丢弃并重录和原始 MP4 快速可用。
- [ ] 完成自动缩放、平滑光标、点击效果、画中画、转场、字幕、转写、整理、视频标注和三档导出的成片视觉验收。
  - [x] 安装版真实跨 Finder/Safari 录制 75.233 秒；原片和智能效果成片均为 60.000 FPS，自动运镜、光标、点击反馈和画布经最终编码反事实对照全部验证生效，健康报告 0 警告。
  - [x] 真机把倍率从 1.60× 改为 1.65× 后，UI、计划倍率和全部自动点击镜头关键帧同步变化；恢复 1.60× 后 11 个转场通过舒适度审计。
  - [x] 构建 `20260813090147` 通过真实项目 G3 三档成片硬门：原画/平衡/轻量分别实测 60/30/24 FPS，每档自动运镜、光标、点击反馈和画布 4/4 最终编码验证通过，源项目未被改写。
  - [x] 真实 27.205 秒摄像头/麦克风项目的画布、摄像头画中画与音频混音均通过最终媒体验证；混音比较 600000 个 PCM 样本，音频漂移 13.3 ms。
  - [x] 字幕编排/渲染/编码用真实录屏和已校对诊断文本通过最终媒体反事实验证；H.264 半透明玻璃与字形边缘使用方向相关性和投影强度双证据，且故意不烧入字幕的编码反例必须失败。报告明确不将该证据计作语音识别成功。
  - [x] 本机中文转写受控门通过：6.575 秒非静音语音生成 9 个合法时码片段，8/8 核心词命中且无云端回退；空白历史麦克风素材正确返回无讲话，不伪造成功。
  - [x] 同一真实识别结果生成 2 个标签、1 个要点和 1 个合法章节，本地整理结构门通过；报告不保存识别原文。
  - [x] 视频矢量标注加入独立最终媒体反事实门；真实录屏中变化 8874 像素、相似度增益 0.970，60 FPS 成片验证通过。
  - [x] 最新安装构建 `20260813093933` 通过真实项目编辑器操作—保存—重开—导出一致性门：14/14 项 UI 模型操作进入计划并在重开编辑器后恢复，字幕、整理结果等三类资产重新加载一致，60 FPS 成片六项效果 6/6 验证；原项目 10 文件、133167521 字节的 SHA-256 前后一致。
  - [x] 最终预览与精确编辑计划/字幕源摘要绑定；摘要一致才可导出，缺失摘要的旧报告或只改变一个参数的旧预览都会强制重渲染。真实安装版 G3 新鲜度正反例均通过。
  - [x] 真实可见 UI 完成倍率 1.60×→1.65×、自动保存、媒体验证、关闭重开和系统保存面板导出；重开参数、计划、24 个镜头关键帧一致，导出 MP4 与已验证预览逐字节相同。测试后 UI 恢复 1.60×并重新完成 60 FPS 验证。
  - [x] 最新安装构建 `20260813101241` 通过真实可见 UI 的转写→整理→字幕校正→保存重开→原画 60 FPS 混音→系统面板导出链；短音轨先因 8.005 秒音画漂移被正确拒绝，完整成片的字幕、运镜、光标、点击、画布和混音 6/6 最终验证，漂移 1.67 ms、0 警告，导出与验证预览逐字节相同。该证据为受控合成语音，不替代下一项自然人盲测。
  - [x] 大分辨率 H.264 B 帧效果成片在音频混合后由 60 FPS 静默降为 30 FPS 的缺陷已复现并修复；新增 2560×1664、60 FPS 合成→混音回归，原画预设要求实测不低于 58 FPS。
  - [x] 最终安装构建重新跑真实 60 FPS 项目 G3 round-trip：14 项编辑器操作、保存重开、七项最终效果、计划摘要新鲜度和源项目不变全部通过，不复用旧构建报告；构建身份以 `g3-project-roundtrip-installed-final.json` 为准。
  - [x] 编辑器与截图工具的关键滑块已显式暴露可访问名称和值；高对比度、降低透明度、减少动态效果快照与两条可访问语义回归通过。
  - [x] 最新安装构建 `20260831074838` 的扩展短时运行时 AX 审计通过：`accessibilityTrusted=true`、4 个窗口、226 个元素、19/19 个关键 Group/Slider/Button/TextField 检查通过（含录制浮标详情、隐藏、暂停、停止、素材库搜索框、录屏“复制文件”、系统分享和 Finder 定位）；操作中心主操作卡、首次启动权限行、编辑器选择控件、时间线动作、录屏设置开始按钮、截图敏感信息复核入口、社交画幅安全区参考、素材库搜索框和素材库卡片拖拽提示同时补齐名称、用途值、状态和触发提示。该候选使用 `LENS_CODESIGN_TIMESTAMP=none` 的显式 development 回退；非法值与 beta/release 组合会在构建前拒绝，正式发布仍需 Developer ID 与公证。报告为 `Build/Quality/g4-accessibility-runtime-latest.json`。该项不替代真人 VoiceOver 与完整键盘焦点验收。
  - [ ] 用自然人真实讲解完成转写→保存→整理→字幕校对的 UI 全链路和盲测，并人工验收视频标注观感。
- [x] 完成 10 分钟连续四轨录制、成片落盘，以及录制中强退后的原始分片自动恢复与预览重生。
- [ ] 完成磁盘不足、设备拔出和系统流中断的短时真机恢复验收；一小时耐久门已有历史证据，本轮不再追加同类长时测试。
  - [x] 可丢弃 256 MB 镜像内真实 `ENOSPC` 后恢复可播、非静音、音画同步双轨。
  - [x] 安装版录制中 `SIGKILL` 后仅损失最后一个五秒分片边界，项目可自动恢复。
  - [x] 历史安装版一小时严格门通过（本轮不再追加）：3600.077 秒、215543 帧（99.79%）、60.000 FPS、0 丢帧；180002 个系统声音频样本全链路一致且无积压，24 个音频窗口连续非静音、24/24 动态画面不同；主线程 P95 16.63 ms、最大 21.11 ms，峰值物理内存约 174.7 MiB，停止到可播放 5.047 秒。
  - [x] 独立窗口 owning process 终止后 24.4 ms 检出来源消失，安全收尾的物理双轨媒体可播且音频连续。
  - [x] 缺失屏幕媒体的不可恢复旧项目只尝试一次后标为 failed，完整项目包仍保留，后续启动不再重复恢复。
  - [x] 麦克风/摄像头启动竞态或中途异常已改为只降级可选轨，主屏幕录制继续；暂停/继续不重开本次已失败设备，下次新录制重新探测，重复回调去重且健康报告明确记录中断。
  - [ ] 实体麦克风和摄像头拔出的真机门通过。
- [ ] 在浅色/深色、降低透明度、增强对比度、减少动态效果、VoiceOver 和键盘导航下完成无障碍验收。
  - [x] 深色高对比度、降低透明度和减少动态效果的 Action Center/编辑器快照通过；关键滑块名称和值的源码门通过。
  - [x] 新增 `Scripts/run-g4-accessibility-audit.sh`：安装版宿主打开真实截图/视频编辑器，独立进程从 macOS AX API 核验关键 Group/Slider/Button 的名称、值和 Press 动作；锁屏时稳定返回 77 并写出 `blocked/screenLocked`，不伪报成功。
  - [ ] 解锁桌面后用实际 AX 树复核新安装构建，并由真人完成 VoiceOver 主路径与完整键盘焦点顺序。

## 5. 安装、升级与恢复

- [ ] 在干净用户账户完成首次安装、权限引导、首张截图、首次录屏和首次导出。
- [x] 用旧版原始偏好键和录屏/截图黄金项目验证快捷键、媒体偏好、旧项目与索引兼容，不发生静默重置或项目改写。
- [ ] 从上一个公开版本做真实覆盖升级并复核权限；首个公开版本发布前暂无上一公开安装包可测。
- [x] 验证项目包只读、磁盘写入失败与损坏索引时不改写原始项目，并提供复制完整项目包、恢复权限/备份或释放空间的恢复引导。
- [ ] 在真实系统权限中复测未授权、授权被撤回和 App 替换签名的恢复引导。
- [x] 验证卸载说明不会误删 `~/Pictures/Lens/` 中的用户项目，完全清理需显式列出数据目录。

## 6. 签名、公证与分发

- [ ] 通过 `LENS_SIGNING_IDENTITY`、`LENS_VERSION` 和 `LENS_BUILD_NUMBER` 使用固定 Bundle ID `app.lens.mac` 与 Developer ID Application 证书生成 Release App。
- [ ] `codesign --verify --deep --strict --verbose=2` 已通过本地签名 App；`spctl --assess --type execute --verbose=4` 按预期拒绝本地 `PathShot Local Code Signing`，待 Developer ID Application 证书后重跑 Gatekeeper。
- [ ] 使用 `LENS_NOTARY_PROFILE` 指定钥匙串 profile，由打包脚本用 `notarytool` 提交并等待 Accepted，再用 `stapler` 固定与验证票据；断网状态下复核固定结果。
- [ ] 生成可安装 DMG 或签名 PKG，在另一台未安装开发证书的 Mac 上验证。
- [ ] 复核 `release.json` 中工作区为 `clean`、签名为 `developer-id`、Team ID 正确、App/DMG 公证均为 `Accepted`，并确认版本、构建号、最低系统、Mach-O UUID、制品尺寸、SHA-256 和公证 request ID 与本次发布一致。
- [ ] 配置自动更新前完成签名键管理、更新包签名、降级/回滚和断网/损坏更新包测试。

## 7. 发布与回滚

- [x] 打包脚本从 `release.json` 与 SHA-256 清单自动生成发布说明，列出已知问题、权限需求、数据路径、项目格式兼容性和核心制品校验和；发布说明自身也进入校验和清单。
- [x] 本地候选已完成不可覆盖归档与独立复验；同 commit 重复归档被拒绝，篡改归档内发布说明后以状态 65 拒绝，用户项目数量保持不变。
- [x] 发布状态说明与安全公告模板已固定影响范围、用户数据/权限、缓解措施、修复身份、更新时间、协调披露和脱敏检查，并纳入公开源码审计。
- [ ] 上传 App/安装包、dSYM、`release.json`、校验和文件与发布说明；本地不可覆盖归档与独立复验已自动化，公开托管平台的不可变备份仍待真实发布。
- [ ] 公开下载后从公网重新下载，比对 SHA-256，完成安装、启动、捕获、导出和更新冒烟。
- [ ] 按 `docs/发布撤回与回滚手册.md` 演练撤回下载、发布更高构建号的回滚版本、保留用户原始项目和发布安全公告。
