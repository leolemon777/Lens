# 屏迹品牌资产说明

## App 图标

- 源文件：`Assets/AppIcon.png`
- 用途：macOS App 图标唯一主图；构建脚本从该文件生成标准多尺寸 `AppIcon.icns`，派生文件不进入 Git。
- 规格：1024×1024、RGB、不透明 PNG。
- SHA-256：`b7c0307b815edbbc8829af9557530a0fd66cc0a18b334482263e4f5ec146d172`
- 设计语义：四角取景框表示精确捕获；贯穿画面的单条青色轨迹表示屏幕操作历史、平滑光标和自动运镜；烟黑玻璃与银白边缘对应“迹光玻璃”视觉规范。
- 来源：2026-08-10 为屏迹项目使用 OpenAI 内置图像生成工具原创生成，未使用第三方参考图、Logo、字体或素材。公开发布前仍应由项目所有者确认所使用服务条款、商标检索和最终品牌决策。

生成使用的最终提示词：

```text
Use case: logo-brand
Asset type: production macOS application icon master, square 1024×1024
Primary request: Create an original app icon for “屏迹 / ScreenTrace”, a native macOS screenshot and screen-recording utility. The symbol should communicate a precise captured frame plus a graceful recorded motion trace: one confident continuous luminous line entering or sweeping through a minimal crop-frame/window shape, suggesting capture, history, and smooth cursor movement without depicting a literal mouse pointer.
Style/medium: premium macOS icon, restrained dimensional glass and finely machined metal, crisp vector-friendly geometry rendered with subtle physical depth; quiet, precise, instantaneous, first-party quality.
Composition/framing: centered single bold symbol, strong silhouette, generous internal padding, readable at 16 px; macOS rounded-square app icon presentation, no separate floating background outside the icon.
Lighting/mood: soft neutral studio light, controlled highlights, calm and professional.
Color palette: neutral silver-white and smoked graphite; one restrained cool cyan trace highlight; no cheap blue-purple neon, no rainbow gradient.
Materials/textures: translucent smoked glass used as a functional surface, fine bright edge, subtle shadow, minimal texture.
Constraints: no text, no letters, no Chinese characters, no numbers, no watermark, no Apple logo, no literal camera body, no video play triangle, no clutter, no multiple tiny elements, no photorealistic desktop screenshot. Keep the design original and suitable for commercial redistribution. Full-bleed opaque square image.
```

## 变更规则

- 替换主图时必须同步更新本文件和 `Scripts/audit-public-source.sh` 中的 SHA-256，并重新检查 16、32、128 和 1024 像素显示效果。
- 不得把竞品截图、竞品 Logo、Apple Logo、未获授权字体或模板素材合入图标。
- 发布包应检查 `Contents/Resources/AppIcon.icns` 非空、`CFBundleIconFile` 指向 `AppIcon`，且签名后资源校验通过。
