# 屏迹 1.0：产品与研发总计划

版本：v0.51
日期：2026-08-10
状态：M0 已完成；macOS 可运行纵向切片已打通；M1–M7 仍按里程碑补齐中

## 0. 当前执行快照

当前版本已经证明核心技术链路可行，但它仍是纵向切片，不等同于 1.0 功能完成。

| 里程碑 | 当前状态 | 已经落地 | 仍需完成 |
|---|---|---|---|
| M0 产品与架构基线 | 已完成 | 产品原则、竞品矩阵、PathShot 审计、UI 规范、项目格式草案 | 随实现持续校正指标 |
| M1 UI/UX 原型 | 进行中 | 原生迹光玻璃操作中心、录制控制条、Quick Access、贴图窗、Toast、含可编辑/恢复默认快捷键和本地语音识别的权限中心、原生屏迹库、统一视频编辑器、摄像头画布与视频标注直接操控；主路径图标按钮、动态录制状态、电平/磁盘状态和画布控件的辅助功能语义，以及编辑器保存/撤销/关闭等键盘入口；关闭窗口后从 Finder/Dock 再打开可重新显示操作中心 | 完整异常流、VoiceOver 逐项朗读、真机手势与可用性验证 |
| M2 技术基础 | 进行中 | Swift/AppKit/SwiftUI 工程、可持久化双主快捷键、无 Fn 双备用、Carbon 注册占用回退、项目包、恢复机制、权限状态、隐私白名单 JSONL 日志/轮转/诊断摘要、异常退出会话检测、ScreenTrace `.ips` 脱敏崩溃指纹、Release dSYM 生成/UUID 校验/zip 归档、UUID/Bundle 严格匹配的离线 `atos` 符号化、版本化 worker 握手/能力协商/长度前缀 JSON 帧；八类可移植 JSON 的中央 schema 注册表、未来/畸形版本拒绝和旧版录屏/截图黄金样本只读兼容测试，测试底座 | 物理键盘/输入法兼容矩阵、Rust worker 与运行时适配器、Swift/Rust 黄金一致性测试 |
| M3 截图 Alpha | 进行中 | 区域/独立窗口/多窗口/当前屏幕、显示器/普通窗口边缘自动吸附与辅助线、Option 临时绕过、方向键 1pt/10pt 精调、窗口层级选择、跨屏多选/反选、后到前透明合成、截图来源元数据、PNG、剪贴板、自动保存、Quick Access、本地 Vision OCR、矩形/椭圆/箭头/自由画笔/高亮/自动编号/文字/模糊/像素化九类对象化标注、选择/移动/缩放/换色/删除与非破坏性渲染、渐变/纯色背景、四组预设、留白/圆角/阴影、自动/1:1/4:3/16:9/9:16 无裁切画布、PNG/JPEG 导出、手动滚动+自动取帧长截图、固定页头去重、纵向重叠对齐、源帧/拼接计划归档、可复制/缩放/调透明度/锁定/Finder 定位的悬停贴图工具条、历史浏览与标题/OCR 搜索 | 多窗口的多屏/不同缩放真机回归、长截图的真实浏览器/普通 App 兼容矩阵、捕获/贴图/背景编辑手感与真实权限 E2E |
| M4 录屏 Alpha | 进行中 | ScreenCaptureKit 区域/独立窗口/当前显示器原始录屏、可持久化 30/60 FPS 与帧率元数据、系统音频、独立麦克风 CAF 轨、独立摄像头 MOV 轨、真实暂停/继续、二次确认后安全停止并移入废纸篓的同来源重录、三轨分片索引、首段归档、无重编码视频拼接、PCM 麦克风拼接、无暂停事件时间轴、指针/点击/隐私收敛快捷键/前台应用开放事件轨、双路实时音频电平、项目磁盘可用空间显示、5 GiB 预警与 1 GiB 自动安全停止、设备提前停止检测、持久媒体偏好与按需授权、来源元数据、动态窗口指针归一化、原始素材保护 | 重录与事件轨真机验证，以及长时、设备拔出、系统流中断与磁盘异常 E2E |
| M5 自动成片与编辑器 | 进行中 | 自动运镜与光标/点击效果、背景/圆角/阴影、人像画中画、旁白检测与自动混音、本地人声降噪/K 加权响度统一、自动 MP4 预览；开放非破坏性时间线、入/出点、分割、片段移出、0.5×–3× 变速、直接切换/交叉叠化/淡入黑场真实重叠转场、四轨同步重生；玻璃字幕、源时码修改、播放头分割、合并/删除、逐条校对与烧录；原画/平衡/轻量三档导出；摄像头真实比例画布拖放/缩放、源时间关键帧标记/跳转/五种过渡、字幕与点击焦点平滑避让；九类源时间视频标注、画布直接绘制/移动/缩放、时间线区间、淡入淡出与逐帧导出 | 解锁后真机全链路与成片视觉验收 |
| M6 屏迹库与 AI | 进行中 | 可损坏重建的增量持久索引、截图/录屏统一历史、标题/OCR/转写/整理统一搜索、开放转写与 `insights.json` 格式、普通截图后台 OCR、录屏自动串行转写、可持久化六档转写语言、抽取式标题/摘要/标签/要点/章节、敏感信息脱敏提示，以及可保留的人工校正层 | 可选生成式模型、语义聚类与批量整理 |
| M7 分享、开源与公开测试 | 进行中 | 隐私/安全说明、贡献指南、Issue/PR 模板、安装/权限/升级/卸载指南、发布检查清单与不触碰用户项目的撤回/回滚手册；可配置版本/Developer ID/钥匙串公证 profile 的 DMG + App zip + dSYM + SHA-256 打包链，以及记录 Git、UUID、签名、公证和制品摘要的机器可读发布清单；从 App zip、dSYM 和只读 DMG 独立反查摘要、元数据、UUID、图标、签名、公证、Staple 与 Gatekeeper 的公开发布验收器；带固定来源/摘要审计与多尺寸构建验证的正式 App 图标；第三方依赖/素材清单与公开源码媒体、项目、崩溃、发布、签名、超大/非文本文件和常见秘密格式自动审计 | 许可证决策、最终品牌确认、真实 Developer ID 签名/公证凭据实测、自动更新、公开下载与新机验收 |
| M8 Beta 与 1.0 | 未开始 | 架构与接口预留 | Beta 反馈收敛、项目格式稳定、中英文完善、1.0 与 Windows 捕获适配计划 |

当前自动化基线：253 项测试全部通过；Debug/Release 零警告构建、App 打包、Info.plist 与本地 ad-hoc 代码签名结构校验通过。

Vision OCR、九类对象标注与变换、固定页头长截图拼接、重复帧拒绝、输出上限、取帧/收尾并发、源帧与拼接计划原子归档、三类录屏来源坐标/Retina 尺寸、合成麦克风 CAF 写入、双路电平响应、旁白活动检测与压低包络、系统声/麦克风实际混合导出、合成摄像头视频轨校验、媒体偏好、画中画布局计划保留、圆形/圆角矩形画中画像素输出、镜像与四角锚点、系统音频保留、摄像头异常回退、录制分片索引、首段归档、单段恢复、视频/麦克风多段合并、暂停计时、旧项目兼容、容错历史索引、标题/OCR 搜索、项目写入和 Retina UI 快照均有集成测试。

开放非破坏性时间线的分割、修剪、片段移出、变速、真实重叠转场与时间映射，统一编辑器的状态/快照，以及屏幕、摄像头、系统声、麦克风按编辑后时间线同步重生已有自动测试。交叉叠化使用红/蓝 H.264 中点混色断言，淡入黑场使用真实黑帧断言，四轨转场后的时长、音频轨和画中画像素也有集成验证。声音链路以 1 kHz 参考音验证 K 加权响度，以低频轰鸣、宽带噪声和分段语音验证噪声区衰减、信噪比提升、目标 LUFS、精确时长和原始轨不变。摄像头画布拖放/缩放的单手势撤销、源时间关键帧在剪切/变速/重排/重复片段中的映射、五种过渡、非 16:9 预览定位、字幕预动/回落和点击焦点避让，以及真实 H.264 中的动态人像像素位置均有自动测试。持久索引的增量失效、损坏重建与删除清理，转写文档的归一化/项目往返/搜索，Apple Speech 结果到开放格式的映射，长音轨分片导出、重叠边界归属和合并去重，以及中日文断句、裁切/重排/变速后字幕映射、源时码限幅、播放头分割、合并/删除、玻璃背景烧录、校对副本与编辑器状态均有自动测试。三档导出另以 60 fps 合成 H.264 验证轻量预设真实降至 24 fps，并覆盖旧项目原画兼容、画中画码率和系统声/麦克风混音链路；字幕与导出底部检查器已在 1260×780 原生快照中人工验收，时间线标注带已收敛在轨道边界内，不再遮挡速度控制。本地整理还覆盖开放格式往返、索引失效、人工覆盖层、跨转写片段敏感信息、Luhn 卡号校验、原值不落盘和玻璃详情面板快照。涉及系统屏幕录制与本地语音识别权限、真实浏览器/普通 App 长截图、真实麦克风/摄像头设备、真实暂停/继续、真实选区 OCR/自动整理/转写/字幕、窗口截图/录屏、编辑器/屏迹库交互手势和物理 `Fn` 键的验收仍必须在已授权且解锁的 Mac 上完成。

视频标注另外覆盖开放格式往返、旧计划兼容、越界时间归一化、剪切/变速/重排/重复片段映射、交叉叠化/淡入黑场权重同步、单手势撤销、真实 H.264 活动区间像素断言，以及模糊/像素化区域隔离测试。

多窗口截图现已使用独立窗口无阴影捕获与统一逻辑坐标画布，按桌面窗口层级从后向前透明合成；跨显示器选择状态在所有捕获层同步，单击可反选，Return 执行，Esc 取消。开放项目 Manifest 已升至 `0.8`，新增截图来源模式、显示器/窗口 ID、全局边界与区域来源；旧项目缺少该可选字段时继续兼容。自动测试覆盖跨屏负坐标联合边界、稳定层级顺序、真实红/蓝像素覆盖和项目落盘往返。

贴图窗现已移除容易误触的“双击关闭”，改为移入显示原生材质工具条：复制、缩小、放大、四档透明度循环、位置锁定和关闭均可直接操作；右键菜单补齐精确透明度、恢复适合大小和 Finder 定位，Command+C/加减号/0/W 与 Esc 可键盘操作。超大图的缩放上下限相对适合屏幕比例计算，避免缩小反向跳大；状态边界、透明度循环、菜单结构、辅助功能标签和 AppKit 渲染已有自动测试。

区域截图、区域录制和长截图选区现统一读取普通窗口边界，在每块显示器的捕获层初始化时预换算为本地坐标；拖动端点进入 8pt 阈值时分别吸附 X/Y 边缘并绘制虚线辅助线。Option 可临时绕过吸附；鼠标仍按住时方向键按 1pt 微调，Shift+方向键按 10pt 微调，松手不会被原始鼠标坐标覆盖。Core 算法覆盖最近边缘、显示器边界、阈值关闭，AppKit 事件测试覆盖真实拖动、Option 和方向键交付矩形。

截图编辑计划现已升至 `0.3`，背景画布、颜色、留白、圆角、阴影与画布比例均与标注一起非破坏保存；旧 `0.2` 计划缺少画布字段时按未开启背景读取。共享布局器保证 1:1、4:3、16:9 和 9:16 只扩展画布而不裁切原图；渲染测试对输出尺寸、背景/原图像素、PNG/JPEG 可解码性、项目包往返、单次滑块撤销与 1080×720 编辑器快照均有断言。

辅助功能静态收尾已覆盖操作中心、设置与权限、Quick Access、屏迹库、录制控制条、长截图、截图标注和视频编辑器：仅图标按钮提供明确名称，录制状态/时长、双路电平、摄像头分轨和磁盘空间提供动态值，颜色与背景预设提供用途名称，时间线片段、标注区间、关键帧与字幕项提供时码语义；截图/长截图、设置、Quick Access、屏迹库和视频编辑器补齐 Escape/默认操作，视频编辑器补齐 Command-Z、Shift-Command-Z 与 Command-S。动态录制语义有模型级测试，改动后的录制控制条快照已复核；XCTest 无辅助功能客户端时 SwiftUI AX 子树为空，因此真实 VoiceOver 树、焦点顺序和朗读仍保留为解锁真机门槛，不用静态修饰符替代实际验收。

本地诊断链路已覆盖事件码与元数据白名单、路径/正文拒绝、NSError 仅保留 domain/code、JSONL 写入与 512 KiB 双文件轮转、最近事件读取和隐私收敛摘要测试。会话健康标记可区分正常退出与崩溃/强退，损坏或被塞入私密内容的标记只会被安全覆盖。真实 Release App Bundle 已验证启动日志落盘、`SIGKILL` 后重启写入 `app.previous_session_unclean`、设置页底部布局及“诊断摘要已复制”状态反馈。

ScreenTrace `.ips` 扫描仅接受匹配 Bundle ID 的普通文件，拒绝其他应用、符号链接和超过 10 MiB 的报告；仅提取 incident UUID、时间、版本、异常类型/信号、终止命名空间/代码和故障线程号，调用栈、路径、进程参数与线程正文均不进入指纹或诊断摘要。

核心 worker 边界 v1 已固定协议版本、UUID 请求关联、能力去重/排序协商、稳定错误码，以及 `4 字节大端长度 + UTF-8 JSON` 帧。解码器支持分片和连续多帧，在 JSON 解码前拒绝超过 1 MiB 的声明。当前机器没有 Rust 工具链，因此生产主链仍使用 Swift，未提交无法编译验证的 worker 空壳。

`Scripts/build-release-artifacts.sh` 已实际生成 Release App 和 `Build/Symbols/ScreenTrace-0.1.0-1.dSYM.zip`，通过 App/Info.plist/签名结构、zip 完整性与 Mach-O UUID 一致性校验，并输出 SHA-256。当次 App 与 dSYM UUID 均为 `DD164C85-0106-38DF-8B8D-F63E44323285`，归档 SHA-256 为 `e3450c0849716df05a0e5ae8add7c41ad7cbd9dd9b7fdb701e97a2465ab9ab06`；本机现有旧 `.ips` 属于不同 UUID，明确拒绝用当前符号误解旧崩溃。

`Scripts/package-macos-release.sh` 已在 ad-hoc 模式生成带 Applications 链接的 DMG、App zip、dSYM 副本和统一 SHA-256 清单；DMG 创建前后均通过 `hdiutil verify`，挂载后 App 再次通过严格验签与 Info.plist 检查，三个发布文件均通过校验和反向复核。脚本支持先公证 App zip 并 Staple App，再生成/签名/公证/Staple DMG；当前未提供 Developer ID 与钥匙串 profile，因此不宣称已完成公证。

真机从 Finder 预览构建产物后复现出 `com.apple.FinderInfo`/用户标签残留导致后续 `codesign` 拒绝重签名；`build-app.sh` 现先严格校验目标只能是仓库内 `Build/ScreenTrace.app`，再完整重建这一生成目录，避免旧签名、资源与 Finder 扩展属性混入新包。带污染构建产物的反向复现和修复后的 App/DMG 重建、严格验签均已通过。

`Scripts/symbolicate-screen-trace-crash.sh` 已实现离线符号化：先校验 `.ips` 的 Bundle ID 与 slice UUID，再定位故障线程、ScreenTrace image index/base/offset，只对自身帧调用 `atos`。旧 `.ips` 与当前 dSYM 的反向用例以状态 67 拒绝；同 UUID 合成报告正向用例成功还原到 Swift 符号和 `CaptionCuePlanner.swift:245`。

本机旧 `.ips` 的已有符号直接指向 `PermissionCenterModel.performPrimaryAction(for:)` 中的语音识别授权回调：TCC 从后台队列调用了在 `@MainActor` 上下文生成的闭包，在进入闭包体前即触发 Swift executor trap。回调现已改为显式非隔离 `@Sendable` 桥，再切回 MainActor 刷新；使用 `Task.detached` 的回归测试已验证后台入口不崩溃且最终回到主 actor。

## 1. 产品定义

> 屏迹是一款本地优先、计划开源、AI 原生的截图与录屏工作台：一按即捕捉，停下即成品，之后永远找得到。

屏迹 1.0 解决四件事：

1. **快捕获**：快捷键出现捕获层，截图和录屏都不需要先打开主窗口。
2. **立即可用**：截图立即进入剪贴板；录屏停止后原始文件立即可播放。
3. **自动成片**：录屏自动完成光标、缩放、背景、音频和字幕的第一版处理。
4. **智能整理**：截图和录屏统一进入屏迹库，由 OCR、转写与 AI 自动命名、搜索和整理。

## 2. 1.0 的产品原则

### 2.1 捕获不能被编辑器拖慢

- 捕获进程常驻但保持轻量。
- 完整编辑器按需加载。
- AI 和自动剪辑都在捕获完成后异步执行。
- 即使自动处理失败，原始截图和录屏也必须可用。

### 2.2 默认完成，用户负责修正

- 默认给出可直接分享的结果。
- 编辑器用于修正自动结果，而不是每次捕获的必经步骤。
- 所有自动处理均非破坏性、可关闭、可撤销。

### 2.3 本地优先，不等于拒绝分享

- 无账号即可完成捕获、编辑、整理和导出。
- 默认不上传媒体。
- 分享采用可选的本地文件、iCloud、用户自有 S3/R2 或未来的官方服务。

### 2.4 开放项目格式

- 原始媒体、事件、转写、编辑计划分离。
- 项目包可以被第三方工具、插件和 AI Agent 读取。
- 停止维护后，用户仍然能取回原始素材和编辑信息。

### 2.5 macOS 体验优先，边界从第一天支持 Windows

- macOS 捕获和浮层使用原生技术保证速度与系统感。
- 项目模型、媒体处理、自动编辑规则尽量跨平台。
- Windows 后续只替换平台捕获适配器，不重写整个产品。

### 2.6 吸收竞品优势，但不做功能拼盘

- PathShot、HuaStudio、Kite、ShotGlass，以及调研中的 Screen Studio、FocuSee、CleanShot、PixPin、Tella、Loom、Cap 等产品，其核心优势必须进入《全景功能与竞品能力吸收矩阵》；
- 每项能力都必须明确：参考价值、屏迹形态、所在里程碑、验收方法和隐私/性能代价；
- “做进去”指独立实现同类价值与效果，不复制对方的界面、素材、品牌表达或受许可证限制的代码；
- 全部能力可以存在，但不能全部同时出现在默认界面；高频动作一键完成，高级能力渐进展开；
- 1.0 优先完成可靠的完整闭环，复杂演示能力进入已排期的 1.x 路线，不成为永远搁置的“以后再说”。

### 2.7 原生“迹光玻璃”是产品级约束

- 屏迹在 macOS 上应像 Apple 第一方工具自然生长出来，而不是普通窗口叠加一层模糊；
- 捕获层、`Fn + Space` 操作中心、录制控制条、Quick Access、编辑器外壳和设置优先使用 Swift、AppKit 与 SwiftUI 原生实现；
- 在 macOS 26 使用系统原生玻璃效果、交互态和形变能力，旧系统使用原生视觉材质降级；
- 玻璃只承担导航、控制和状态，截图或视频内容本身必须保持清晰、稳定、色彩可信；
- 必须支持浅色、深色、降低透明度、增强对比度和减少动态效果等辅助功能设置；
- 视觉默认克制、安静、内容自适应，不使用廉价霓虹、过度描边或无意义的持续动画。

## 3. 产品表面

屏迹 1.0 由六个主要表面构成：

1. **捕获层**：`Fn + Control` 直接进入快速截图，`Fn + Space` 唤起完整操作中心。
2. **录制控制条**：来源、音频电平、暂停、停止、时间和磁盘状态。
3. **Quick Access**：捕获完成后的悬浮缩略图与最近素材架。
4. **统一编辑器**：截图画布与视频时间线，共享背景、标注和导出体系。
5. **屏迹库**：历史、搜索、OCR、转写、标签、项目和 AI。
6. **设置与权限中心**：快捷键、保存、录制、隐私、AI、分享和系统权限。

### 3.1 双入口启动模型

屏迹采用“直接动作 + 操作中心”两个入口，避免简单截图和复杂操作互相拖累。

#### `Fn + Control`：快速截屏

- 按下后立即进入智能区域截图；
- 悬停窗口时自动识别窗口边界；
- 拖动时进入自由区域选择；
- 完成后立即复制到剪贴板，并按设置自动保存；
- 不弹出模式选择页，不加载完整编辑器；
- Quick Access 只在捕获完成后轻量出现。

这条路径服务于最高频任务，目标是形成肌肉记忆：

```text
Fn + Control → 选区 → 完成 → 已经可以粘贴
```

#### `Fn + Space`：屏迹操作中心

`Fn + Space` 唤起一个由常驻捕获进程提供的轻量浮层，而不是直接启动完整主窗口。

浮层第一版包含：

- 区域、窗口、全屏和多窗口截图；
- 区域、窗口和全屏录制；
- 长截图、OCR、贴图；
- 最近几条截图和录屏；
- 打开编辑器、屏迹库和设置；
- 对最近素材执行复制、分享、AI 整理等上下文操作。

行为规则：

- 再按一次 `Fn + Space` 或按 `Esc` 关闭；
- 支持键盘继续选择动作，避免必须使用鼠标；
- 打开时优先显示最近操作和当前上下文；
- 录制进行中时，`Fn + Space` 优先显示录制状态、暂停和停止；
- 编辑器和屏迹库只在用户明确选择后延迟加载。

```text
Fn + Space
  ├── 新建截图
  ├── 新建录屏
  ├── OCR / 长截图 / 贴图
  ├── 最近屏迹与上下文操作
  └── 编辑器 / 屏迹库 / 设置
```

#### 快捷键兼容要求

- `Fn` 在 macOS 中属于特殊硬件修饰键，需要验证内置键盘、Magic Keyboard 和第三方键盘；
- 默认提供 `Fn` 方案，但所有快捷键必须允许自定义；
- 提供不依赖 `Fn` 的备用快捷键；
- 处理按键长按、重复触发、输入法切换和系统 Globe/Fn 功能冲突；
- 权限中心需要清楚解释全局按键监听所需权限及用途。

## 4. 为什么先做 UI，但不是先画视觉稿

UI 阶段首先解决的是流程、状态和优先级，而不是颜色、玻璃效果和图标。

正确顺序：

1. 用户任务与关键路径；
2. 信息架构；
3. 捕获与录制状态机；
4. 低保真线框；
5. 可点击交互原型；
6. 技术可行性校验；
7. 视觉语言和设计 Token；
8. 高保真组件；
9. 实际产品实现。

UI 原型需要覆盖以下完整流程：

- 区域截图 → 标注 → 剪贴板 → Quick Access；
- 窗口/多窗口截图 → 背景美化 → 拖到其他 App；
- 长截图 → 拼接预览 → 裁切 → 保存；
- 区域录屏 → 检查声音 → 开始 → 暂停 → 停止；
- 停止录屏 → 原始文件可用 → 自动处理 → 快速预览；
- 打开编辑器 → 修正缩放/剪切/字幕 → 导出；
- 打开屏迹库 → 搜索 → AI 整理 → 生成教程或短版本；
- 权限缺失、磁盘不足、录制中断、恢复失败等异常流程。

## 5. UI 阶段同步进行的技术验证

UI 不能脱离真实技术约束，因此交互原型阶段同步完成六个小型技术验证：

1. `Fn + Control`、`Fn + Space` 全局快捷键和原生透明浮层的响应速度；
2. ScreenCaptureKit 截图的窗口、多屏、Retina 和坐标映射；
3. 30/60 FPS 录屏以及系统声音、麦克风分轨；
4. 鼠标位置、点击、形状、键盘和活动窗口事件采集；
5. 视频分片写盘、中断恢复和快速 finalization；
6. Swift 捕获进程、Rust 核心和共享编辑器之间的数据边界。

这些验证不追求完整产品，只负责回答“能不能稳定做到”和“UI 应该如何表现”。

## 6. 技术结构基线

原生 UI 原型与媒体测试已经确认第一版采用以下结构：

```text
ScreenTrace for macOS
Swift + AppKit + SwiftUI
        │
        ├── Capture Agent / Global Hotkeys
        ├── Overlay / Action Center / Quick Access
        ├── ScreenCaptureKit / AVFoundation / Core Image
        └── Native Editor Shell / Settings / Library
                    │
                    ▼
Cross-platform Core
Open project model + deterministic effect plan
        │
        ├── Media metadata
        ├── Event processing
        ├── Auto-camera / cursor / interaction rules
        ├── Recovery / validation
        └── Rust migration boundary for Windows
                    │
          ┌─────────┴─────────┐
          ▼                   ▼
Native Media Renderer     AI Worker
AVFoundation/Core Image   Local models / BYOK
          │                   │
          └─────────┬─────────┘
                    ▼
              Trace Library
          SQLite + project packages
```

macOS 关键表面和编辑器外壳原生优先，不再以 Tauri/React 作为默认方案。跨平台复用的是项目格式、效果计划、事件算法与未来的 Rust 核心，不以牺牲 macOS 原生体验换取同一套 UI。

## 7. 屏迹项目格式草案

每次截图或录屏形成一个 `.screentrace` 项目包：

```text
Example.screentrace/
├── manifest.json
├── raw/
│   ├── screen.mov
│   ├── screenshot.png
│   ├── system-audio.wav
│   ├── microphone.caf
│   ├── camera.mov
│   └── segments/
│       ├── screen-000.mp4
│       ├── screen-001.mp4
│       ├── microphone-000.caf
│       └── camera-000.mov
├── events/
│   ├── pointer.jsonl
│   ├── clicks.jsonl
│   ├── keyboard.jsonl
│   ├── windows.jsonl
│   └── segments.json
├── analysis/
│   ├── ocr.json
│   ├── transcript.json
│   ├── sensitive-data.json
│   └── summary.json
├── edits/
│   └── edit-plan.json
└── previews/
```

图片项目只创建所需文件，不能为了统一格式产生无意义资源。

## 8. 研发里程碑

### M0：产品与架构基线

交付：

- 1.0 功能边界；
- 用户与使用场景；
- 关键体验指标；
- 技术验证清单；
- 开源许可证选择；
- 项目格式 v0 草案；
- PathShot 可复用代码审计。

完成标准：所有团队成员对“第一版做什么、不做什么”理解一致。

### M1：UI/UX 可点击原型

交付：

- 六个产品表面的信息架构；
- 截图、录屏、自动处理和异常状态机；
- 低保真线框；
- 可点击原型；
- 第一版设计语言、颜色、字体、间距和动效原则；
- 关键流程可用性评审。

完成标准：不阅读说明也能完成一次截图、一次录屏和一次导出；简单捕获不会被复杂编辑器干扰。

### M2：技术基础与项目骨架

交付：

- 原生捕获 Agent；
- Rust 核心初版；
- `.screentrace` 项目包读写；
- 日志、崩溃恢复与权限中心；
- 编辑器壳与跨进程通信；
- 基础自动化测试。

完成标准：截图和 10 分钟测试录屏可以可靠写入项目包并重新打开。

### M3：截图完整闭环 Alpha

交付：

- 区域、窗口、全屏、多窗口截图；
- 自动吸附与精细调整；
- 对象化标注、模糊、像素化；
- OCR、长截图、贴图；
- 自动复制、保存、Quick Access；
- 截图历史与基础搜索；
- 背景美化和常用导出。

完成标准：连续完成 100 次截图无错误；多显示器、Retina、不同缩放比例下坐标正确；用户日常截图不需要打开完整编辑器。

### M4：录屏完整闭环 Alpha

交付：

- 区域、窗口、屏幕录制；
- 系统声音、麦克风分轨；
- 摄像头可选；
- 30/60 FPS；
- 暂停、继续、重新开始、丢弃；
- 录制控制条、音频电平和磁盘预警；
- 鼠标、点击、键盘和窗口事件；
- 中断恢复与原始视频快速可用。

完成标准：一小时录制音画同步；强制中断后可以恢复；停止后不等待自动编辑即可播放原始视频。

### M5：自动成片与统一编辑器

交付：

- 自动/手动缩放；
- 光标平滑、替换、大小和点击效果；
- 背景、圆角、阴影、比例；
- 剪切、分割、速度；
- 音频降噪与响度统一；
- 字幕和转写；
- 视频标注；
- 截图与视频共享的资源、设置和导出组件；
- 所有效果非破坏性。

完成标准：典型一分钟演示停止后自动生成可分享草稿；任意自动效果都能单独修改或关闭；原始录制始终保留。

### M6：屏迹库与 AI

交付：

- 截图、视频、OCR、转写统一搜索；
- 自动命名、标签、摘要和章节；
- 敏感信息检测；
- 自然语言编辑计划；
- 录屏转图文步骤；
- Bug 报告、Markdown 教程和短视频生成；
- 本地模型与 BYOK 策略；
- AI 功能可完全关闭。

完成标准：关闭 AI 后核心产品完整可用；开启 AI 后无需上传原始媒体即可完成至少一套本地整理流程。

### M7：分享、开源与公开测试

交付：

- 本地文件、剪贴板与拖拽分享；
- 可选 iCloud/S3/R2；
- 签名、公证、自动更新；
- 隐私说明、许可证、贡献指南；
- 安装与权限引导；
- 性能、兼容性和恢复测试矩阵；
- Public Alpha。

完成标准：新用户可以独立完成安装、授权、截图、录屏、恢复和导出；没有已知的原始素材丢失问题。

### M8：Beta 与 1.0

重点：

- 根据真实工作流删减功能与 UI；
- 修复可靠性和性能问题；
- 稳定公开项目格式；
- 完善中文和英文；
- 制定 Windows 捕获适配计划；
- 发布 1.0。

## 9. 不作为 1.0 首发阻断项，但进入 1.x 路线

- Kite/ShotGlass 级复杂 3D 摄像机和设备摄影棚；1.0 保留轻量透视和 3D；
- AI 配音市场和音乐素材库；
- 团队管理、评论、观看分析和 SSO；
- OBS 级直播；
- Camtasia 级完整非线性编辑器；
- 24 小时被动屏幕记录；
- Windows 正式版；但跨平台接口必须保留，2.0 进入正式执行。

## 10. 关键体验指标

以下是目标值，需要在技术验证后调整：

- 快捷键到捕获层出现：100–150ms；
- 截图到剪贴板可用：约 300ms；
- 停止录屏到原始视频可播放：1 秒内；
- 常驻捕获进程保持低内存、低 CPU、空闲零明显耗电；
- 典型一分钟录屏的自动草稿在后台快速完成；
- 连续 100 次截图无失败；
- 一小时录屏无明显音画漂移；
- 崩溃恢复不丢失已写入的媒体分片；
- 80% 的日常捕获无需打开完整编辑器。

## 11. 主要风险

| 风险 | 应对方式 |
|---|---|
| 功能过多导致启动和 UI 变重 | 捕获 Agent 与编辑器分离；渐进式披露 |
| ScreenCaptureKit 权限和系统版本差异 | 独立权限中心、系统版本测试矩阵、原生 Content Picker 评估 |
| 音画同步和长时间录制丢失 | 分片写盘、独立时钟、恢复与 remux 流程 |
| 自动缩放判断错误 | 事件驱动、置信度、自动结果可编辑 |
| AI 处理泄露内容 | 默认本地、BYOK、逐项授权、AI 可完全关闭 |
| 过早跨平台导致 macOS 体验妥协 | 只共享核心与编辑器；平台捕获层保持原生 |
| 引入不兼容开源代码 | 建立依赖和许可证清单；GPL/AGPL/BSL 项目只在许可允许范围内参考 |

## 12. 立即执行顺序

当前执行采用“可运行纵向切片 + 测试闭环”：

1. PathShot 审计与能力矩阵（已完成）；
2. `Fn + Space` 原生迹光玻璃操作中心（已完成首个可运行版本）；
3. 区域、独立窗口和当前显示器截图、项目包、剪贴板及 Quick Access（已编码，待系统权限后的完整 E2E）；
4. 原生录制控制条、ScreenCaptureKit 原始录屏、事件分轨和中断恢复（首个纵向切片已编码）；
5. 自然模式自动运镜、鼠标平滑、点击反馈、背景、圆角、阴影与 MP4 渲染（首个纵向切片已通过媒体集成测试）；
6. 音频电平/自动混音/本地降噪与响度统一、摄像头合成与画布直接操控/动态避让/源时间关键帧、真实片段重叠转场、九类高级截图与视频标注、滚动长截图、统一视频编辑器、持久素材索引、本机长录屏分段转写、可编辑源时码玻璃字幕、三档导出，以及本地标题/摘要/标签/章节/敏感信息整理已完成首个可测纵向片；当前进入解锁后真机端到端验收；
7. 每项能力执行“编码 → 自动测试 → 本机 UI 测试 → 修复 → 回归测试”；
8. 达到对应里程碑的可靠性门槛后再进入下一发布级别。

第一批 UI 重点只放在三块：

- 捕获层；
- 录制控制条；
- Quick Access。

这三块决定屏迹能否真正做到“快”和“无打扰”，也会决定后续编辑器与素材库的数据入口。
