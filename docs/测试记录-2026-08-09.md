# 屏迹测试记录

日期：2026-08-09  
阶段：macOS 原生纵向切片  

## 结论

当前代码、媒体效果管线和 App Bundle 构建基线全部通过。真实截图/录屏端到端验收仍受 macOS 屏幕录制隐私授权与本机锁屏状态限制，因此不能把纵向切片判定为完整 Alpha。

## 自动化测试

执行：

```bash
swift test
```

结果：172 项测试、0 失败。

覆盖范围：

- `Fn + Control` 与 `Fn + Space` 状态机，包括去重和额外修饰键；
- 多屏区域坐标转换、跨屏窗口裁切、重叠窗口前后层级选择和截图模式稳定性；
- 区域/窗口/显示器录屏模式稳定性、跨屏偏移选区裁切、Retina 偶数编码尺寸和选区外指针拒绝；
- 录屏来源元数据（模式、窗口/显示器、选区、标题、应用名）项目往返，以及 `0.1` 项目向后兼容读取；
- 系统声/麦克风/摄像头偏好的安全默认值与持久化、物理分轨资产登记/移除和 finalize 不恢复失效资产；
- 4,800 帧合成讲解音频写入独立 CAF、声道/采样率/帧数与文件大小检查；
- Float32 双声道/交错 PCM 电平归一化、快速攻击与慢速释放；
- 合成旁白的分段活动检测、相邻区间合并、压低攻击/保持/释放包络与时间轴边界约束；
- 含系统声的合成 H.264 视频与独立麦克风 CAF 实际混合导出，并检查可播放视频轨、音频轨与时长；
- 本地人声处理使用高通、噪声门、柔和动态压缩和峰值保护；合成低频轰鸣/宽带噪声/分段语音验证噪声区衰减、信噪比提升、语音保留、精确时长与原始 CAF 字节不变；
- BS.1770 风格 K 加权、400 ms/75% 重叠门控响度分析以 1 kHz 参考音校准，并验证安静人声归一化到目标 LUFS、增益上限与峰值限幅；
- 开放非破坏性时间线对分割、入/出点、片段移出、速度、源时间↔成片时间映射与损坏数据归一化的往返测试；
- 合成视频按非连续片段与变速输出真实 MP4，并校验系统声、摄像头、麦克风及旁白压低区间与新时间线同步；
- 直接切换、交叉叠化、淡入黑场的非破坏项目格式、旧项目兼容、邻接片段时长限幅、重叠成片时长和主导源时间映射；
- 两轨交替 AVFoundation 合成与真实 H.264 导出：红/蓝片段叠化中点必须同时含两色，黑场转场中点必须为真实黑帧；
- 重叠转场经过自动运镜/光标/背景管线后仍保留像素混合；系统声、独立麦克风和摄像头侧轨按同一重叠布局同步重生；
- 摄像头/麦克风侧轨短于屏幕轨时保留时间线空隙，后续重排片段不会提前造成音画错位；
- 统一编辑器对分割/变速/移出、效果参数、撤销/重做、已保存/未保存边界的状态测试，以及 1260×780 原生编辑器快照；
- 编辑器摄像头画布拖放/缩放、连续手势合并为单次撤销、真实素材比例、摄像头缩略图和丢帧占位容错；
- 摄像头源时间关键帧在剪切/变速/重排/重复片段中的多点映射，时间线菱形标记、前后跳转、新增/更新/删除与五种过渡选择；
- 可丢弃持久索引对 OCR/转写增量失效、损坏索引自动重建、已删除项目清理、符号链接循环/越界防护及搜索结果稳定性的测试；
- `analysis/insights.json` 的开放格式、0.1 向后兼容、原子写入、Manifest 资产登记、持久索引失效与标题/摘要/标签统一搜索；
- 本地抽取式标题、摘要、标签、要点和录屏章节，以及中日文紧凑文本、应用元数据与时间边界的确定性测试；
- 邮箱、电话、Luhn 有效卡号、18 位证件号和凭据检测；跨两个转写片段的敏感值定位，以及整理 JSON 不包含命中原值的隐私断言；
- 人工标题/摘要/标签覆盖层的归一化、搜索、恢复自动结果与旧版整理文档兼容测试；
- 含换行标签、章节、脱敏提示和人工校正状态的 380×520 原生玻璃整理面板快照；
- 开放转写文档对时间片段归一化、项目包原子写入/读取和标题/OCR/转写统一搜索的往返测试；
- Apple Speech 时间片段到设备端转写文档的映射、缺失音轨安全拒绝和语音识别权限状态映射；
- 125 秒音轨的 50 秒/1 秒重叠分片计划、边界中点单一归属、时间偏移和重复词剔除；
- 合成 CAF 指定区间实际导出为 Speech 可读 M4A，并校验音频轨、时长与非空输出；
- 中文紧凑断句、英文标点间距、人工字幕副本与空文本隐藏的非破坏性测试；
- 字幕源时间跨剪切、片段重排与变速映射到成片时间的同步性测试；
- Core Image 字幕叠加的活动区间像素检查，以及真实 H.264 MP4 中玻璃背景/文字只在字幕时段出现的媒体集成测试；
- 字幕开关、三种样式、三个位置、字号、逐条校对副本及撤销的编辑器状态/快照测试；
- 合成摄像头视频的独立视频轨校验、空轨拒绝、默认画中画布局及自动编辑计划更新后的布局保留；
- 暂停/继续分片索引的创建、完成、累计时长、物理轨登记与空分片清理；
- 两段 H.264 的无重编码顺序合并、单个恢复分片重建主文件、首段归档后主文件合并且原始首段不变；
- 两段独立 CAF 的 PCM 拼接、采样率/声道与精确总帧数检查；
- 录制控制条真实暂停/继续计时、过渡态禁用、双路音频电平与暂停视觉快照；
- 圆形画中画遮罩、圆角矩形、四角锚点、镜像、边距与阴影的 Core Image 像素检查；
- 合成屏幕/摄像头视频的实际 H.264 画中画导出与目标色块检查；
- 自由归一化定位、边界夹取、字幕冲突避让、点击焦点远离、字幕前摇/回落平滑量与旧 `0.3` 计划兼容读取；
- 真实 H.264 导出中的摄像头关键帧移动，对前后两帧绿色人像像素质心做定量断言；
- 画中画导出后的系统音频轨保留，以及损坏摄像头轨自动退回屏幕预览；
- 画中画视频编码与系统声 passthrough 回填分离，避免 AVAssetWriter 双输入背压死锁；曾复现失败的媒体测试连续 10 次通过；
- 麦克风/摄像头权限状态映射及权限中心动作呈现；
- OCR 文档序列化、Manifest 资产登记、原始截图不变和错误项目类型拒绝；
- 合成英文文字图片的真实 Apple Vision 识别、文本输出与归一化边界框；
- 截图标注计划对矩形、椭圆、箭头、自由画笔、高亮、自动编号、文字、模糊和像素化九类对象的 Codable 往返和源尺寸约束；
- 矩形、椭圆、箭头、文字的实际像素输出，以及模糊/像素化只影响目标区域；
- 标注保存服务对原图不变、计划文件、渲染预览和 Manifest 资产的完整闭环；
- 标注编辑模型的反向拖动、箭头方向、文字单击、撤销/重做与清空；
- 自由画笔路径采样/简化/命中/移动/缩放、高亮半透明填充与自动编号连续性；
- 带固定页头的合成滚动视口纵向对齐，重复帧去除、页头只保留一次、多段位移累计与最终像素高度检查；
- 长截图的尺寸变化拒绝、内存/像素上限、定时取帧与用户收尾并发、连续两次捕获回调及玻璃控制条快照；
- `raw/scrolling/frame-*.png` 源帧、`events/scrolling-capture.json` 放置/重叠计划与 Manifest 资产的原子归档，重复写入时不破坏已有源帧；
- 共享标注几何层的顶层对象命中、箭头线段命中、画布边界移动、四角缩放、文字字号缩放和箭头端点缩放；
- 编辑器选择模式的顶层对象选择、移动、缩放、换色、删除及逐操作撤销；
- 1080×720 逻辑尺寸下含选中框与控制点的原生编辑器离屏快照，并验证 Retina 2× backing scale；
- 屏迹库对损坏项目的容错扫描、按时间倒序、渲染资产优先和 OCR 读取；
- 标题/OCR 多词搜索、音调符号折叠、截图/录屏类型筛选及数量统计；
- 1020×690 逻辑尺寸下混合截图/录屏屏迹库的原生离屏快照；
- 含截图/录屏双菜单的操作中心快照、截图/录屏选区提示语义，以及显示当前来源的录制控制条快照；
- `.screentrace` 截图/录屏项目生命周期、事件轨、恢复与原始视频校验；
- 自动运镜的点击聚焦、连续点击重定向和边缘约束；
- 光标自适应平滑、坐标约束与 seek-safe 插值；
- 合成测试视频的自动预览导出；
- 背景画布、圆角/阴影、光标与点击反馈的渲染链路；
- 输出视频轨、时长、尺寸和画布像素检查。

## 构建与包检查

以下检查全部通过：

```bash
swift build -c release --product ScreenTrace -Xswiftc -warnings-as-errors
bash Scripts/build-app.sh
plutil -lint Build/ScreenTrace.app/Contents/Info.plist
codesign --verify --deep --strict Build/ScreenTrace.app
```

生成物：`Build/ScreenTrace.app`。当前为本地 ad-hoc 签名，不能替代发布前的 Developer ID 签名与 Apple 公证。

## 已完成的本机 UI 冒烟测试

- 操作中心可启动并显示原生迹光玻璃界面；
- 辅助功能树能识别主要按钮；
- `Esc` 可关闭操作中心；
- 缺少屏幕录制权限时能进入权限说明流程；
- 录制控制条能获得焦点、计时、切换控制状态并关闭；
- 修复了录制控制条没有成为 key window 的问题。

本轮新增的截图模式菜单、窗口悬停层、权限中心、选区 OCR、本机转写、标注编辑器、统一视频编辑器和屏迹库尚未完成 Computer Use 回归：本机处于锁屏状态，自动化遵循安全边界，没有尝试绕过锁屏。

## 仍需人工/真机完成

1. 解锁 Mac，并为 Bundle ID `app.screentrace.mac` 授予屏幕与系统音频录制权限；
2. 使用物理键盘分别验证内置键盘、Magic Keyboard 与第三方键盘的 `Fn + Control`、`Fn + Space`；
3. 验证单屏、多屏、Retina、不同缩放比例和屏幕排列下的截图坐标；
4. 分别完成区域、独立窗口、当前显示器截图 → 剪贴板 → Quick Access → 项目包的端到端验证；
5. 分别完成区域、独立窗口、当前显示器录制 → 暂停/继续多次 → 系统音频/麦克风/摄像头独立分片 → 无暂停事件时间轴 → 原始 MP4 合并 → 自动预览的端到端验证；
6. 完成 Safari/Chrome/普通 App 的区域选择 → 手动滚动 → 自动取帧/去重/拼接 → 完成复制 → 源帧/计划归档的端到端验证；
7. 完成中英文混合选区 → Vision OCR → 剪贴板 → `analysis/ocr.json` 的端到端验证；
8. 完成 Quick Access → 九类标注 → 撤销/重做 → 完成复制 → 再次打开继续编辑的端到端验证；
9. 完成屏迹库 → 标题/标签/摘要/OCR 搜索 → 类型筛选 → 查看/重新整理/人工校正/恢复自动 → 打开/复制/标注/Finder 定位的端到端验证；
10. 完成屏迹库录屏 → 编辑器预览 → 入/出点/分割/变速/移出 → 摄像头拖放/缩放/避让/关键帧 → 保存 → 四轨同步重生的端到端验证；
11. 完成录屏 → 自动串行设备端转写 → `analysis/transcript.json` → 默认玻璃字幕 → `analysis/insights.json` → 编辑器校对/重生 → 屏迹库文字搜索的端到端验证；
12. 进行 10 分钟、1 小时录制，以及强制退出、磁盘不足、设备拔出等恢复测试；
13. 在浅色/深色、降低透明度、增强对比度和减少动态效果下做 UI 验收。

## 当前不应宣称已经完成的能力

- 摄像头在预览画布上的直接拖动/缩放、自动避让和关键帧的解锁后真机手势与成片视觉验收；
- 多窗口截图，以及长截图在真实浏览器/普通 App 中的兼容矩阵；
- 转写语言选择、字幕时码/分句精修、视频标注，以及可选生成式模型和语义聚类；
- 发布级签名、公证与 Windows 版本。
