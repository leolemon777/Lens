# 屏迹测试记录

日期：2026-08-09 至 2026-08-10
阶段：macOS 原生纵向切片  

## 结论

当前代码、媒体效果管线、App Bundle 和开发发布包基线全部通过。解锁 Mac 上的五项权限、内置键盘默认快捷键、当前显示器截图、四轨录屏与暂停继续、设备端转写、字幕、整理、编辑器、真实 OCR 和贴图主路径均已完成纵向验收；开发安装版可继续本机试用。公开 Beta 仍受根许可证、Developer ID/Team ID、公证与 Gatekeeper 阻断，更广的键盘、多屏、浏览器长截图、长时录制、异常恢复、九类标注手势和 VoiceOver 矩阵仍需专项真机验收。

## 自动化测试

执行：

```bash
swift test
```

结果：273 项测试、0 失败。

公开源码审计另执行 `bash Scripts/audit-public-source.sh`：194 个 Git 跟踪公开文件中有 193 个不超过 5 MiB 的文本文件，以及唯一经过固定路径、1024×1024 不透明规格和 SHA-256 审核的 App 图标 PNG；没有远程 Swift package、Git submodule、符号链接、私有媒体/项目/崩溃/发布/签名文件或常见私钥/令牌格式。审计首次抓到测试夹具中一个仿真 AWS Access Key，夹具已改为仍可触发隐私检测但明确不冒充真实凭据的合成值；相关隐私测试继续通过。根目录仍没有 `LICENSE`，脚本会明确提示公开发布仍被许可证决策阻断。

覆盖范围：

- 默认 `Fn + Control`/`Fn + Space`、任意安全自定义组合和无 Fn 备用组合的通用状态机，包括去重、长按、额外修饰键、纯 Fn 修饰键组合、冲突/危险组合拒绝；
- 八类可移植 JSON 的中央 schema 注册表与版本范围；两套旧版录屏/截图黄金样本经真实项目存储读取后全部文件字节不变，未来 0.9 与畸形版本会被明确拒绝且不会进入屏迹库，写入 9.9 文档会在项目发生任何变化前失败，旧版可丢弃索引会升级重建后再缓存；
- 本地诊断事件的代码/元数据白名单、路径与正文拒绝、NSError domain/code 脱敏、JSONL 追加与大小轮转、最近事件顺序和可复制摘要；
- 会话健康标记的首次启动、未清理异常退出检测、正常退出清理与损坏/含隐私内容标记的安全覆盖；
- ScreenTrace `.ips` 脱敏扫描器对 incident/时间/版本/异常/信号/终止代码的白名单提取，调用栈/路径/线程正文排除，以及其他 Bundle、符号链接和超大文件拒绝；
- 核心 worker 协议的版本/请求 UUID/能力去重排序往返，大端长度前缀 JSON 帧的分片保留与连续多帧消费，超过 1 MiB 声明的预分配拒绝和畸形 JSON 拒绝；测试曾抓到并修复 `Data.removeFirst` 后非零 `startIndex` 导致的第二帧 trap；
- Swift stdio worker 适配器使用真实子进程完成握手与能力交集，并覆盖请求不匹配、协议不兼容、worker 稳定错误、未配置/不存在/无效可执行文件、非零退出、超时、stdout 超限和畸形响应的确定性 Swift 回退；stderr、可执行路径和响应正文不进入公开结果；
- 语言无关 worker v1 黄金文件逐字节固定请求、响应和错误的规范 JSON、四字节大端长度及完整帧十六进制；测试曾抓到空可选 `requestID` 应省略而不是编码成 `null` 的跨语言差异；
- 快捷键偏好的安全默认、JSON 持久化、跨重启恢复、损坏/冲突值整体回退和恢复默认；Carbon 修饰键映射、模拟注册占用后的逐项事件监听回退，以及 678×628 可编辑设置页视觉快照；
- 全局快捷键和录制事件监听均在后台回调内把 `NSEvent` 压成 `Sendable` 纯值后再投递主队列；保存真实监听闭包、从后台队列触发、停止后再次触发的回归验证当前代际正常执行且旧代际不触发动作、不继续写入事件轨；
- 录制控制条的暂停/继续、录制/暂停状态、系统声/麦克风电平、磁盘空间和时长朗读值；操作中心、设置、Quick Access、屏迹库、长截图、截图标注与视频编辑器的主路径图标名称、键盘关闭/默认操作，以及视频编辑器保存和撤销/重做快捷键已静态挂载并通过编译与界面快照回归；
- 附件式 App 的标准应用菜单包含关于、隐藏、隐藏其他应用、显示全部和退出，结构测试确认退出项由 Command-Q 触发并指向 AppDelegate；真实构建完成窗口前置、Command-Q、自行退出和活动会话标记清理，未再用强制信号代替正常退出；
- 多屏区域坐标转换、跨屏窗口裁切、重叠窗口前后层级选择和截图模式稳定性；多窗口负坐标联合边界、后到前绘制顺序、红/蓝真实像素覆盖，以及 `multiWindow` 来源元数据项目往返；
- 区域端点到普通窗口/显示器边缘的独立最近吸附、8pt 阈值和关闭路径；CoreGraphics 普通窗口字典过滤，以及真实 AppKit 鼠标拖动、Option 绕过、方向键微调后松手交付矩形；
- 贴图初始适配、相对缩放上下限、四档透明度循环与安全限幅；真实 AppKit 悬停工具条渲染、右键操作结构和窗口辅助功能标签；
- 截图编辑计划 `0.3` 的渐变/纯色画布、颜色、留白、圆角、阴影和五种比例往返，以及旧 `0.2` 无画布字段兼容读取；
- 1:1 画布扩展且不裁切原图的尺寸/像素断言、阴影不污染多窗口截图透明间隙、PNG/JPEG 文件可解码性、JPEG 透明像素白色底合成、项目包样式往返、滑块一次操作一次撤销，以及含 16:9 背景工具条的 1080×720 原生编辑器快照；
- 区域/窗口/显示器录屏模式稳定性、跨屏偏移选区裁切、Retina 偶数编码尺寸和选区外指针拒绝；
- 录屏来源元数据（模式、窗口/显示器、选区、标题、应用名）项目往返，以及 `0.1` 项目向后兼容读取；
- 系统声/麦克风/摄像头偏好的安全默认值与持久化、物理分轨资产登记/移除和 finalize 不恢复失效资产；
- 30/60 FPS 录制偏好的默认值、持久化与无效旧值回退，实际帧率写入捕获元数据，以及旧项目缺失帧率字段时的兼容读取；
- 转写语言的自动默认值、持久化与无效旧值回退，以及简中、繁中、英语、日语、韩语到稳定 Apple Speech locale 的映射；
- `keyboard.jsonl` 与 `windows.jsonl` 的项目创建、Manifest 资产登记和开放 JSONL 往返；真实临时项目中事件记录器启动、脱敏快捷键/应用切换写入和关闭读取集成；普通文字/密码及仅 Option 输入拒绝、快捷键/非文本控制键脱敏标签、修饰键去重，以及前台应用轨不含窗口标题的隐私断言；
- 4,800 帧合成讲解音频写入独立 CAF、声道/采样率/帧数与文件大小检查；
- Float32 双声道/交错 PCM 电平归一化、快速攻击与慢速释放；
- 合成旁白的分段活动检测、相邻区间合并、压低攻击/保持/释放包络与时间轴边界约束；
- 含系统声的合成 H.264 视频与独立麦克风 CAF 实际混合导出，并检查可播放视频轨、音频轨与时长；
- 本地人声处理使用高通、噪声门、柔和动态压缩和峰值保护；合成低频轰鸣/宽带噪声/分段语音验证噪声区衰减、信噪比提升、语音保留、精确时长与原始 CAF 字节不变；
- BS.1770 风格 K 加权、400 ms/75% 重叠门控响度分析以 1 kHz 参考音校准，并验证安静人声归一化到目标 LUFS、增益上限与峰值限幅；
- 开放非破坏性时间线对分割、入/出点、片段移出、速度、源时间↔成片时间映射与损坏数据归一化的往返测试；
- 合成视频按非连续片段与变速输出真实 MP4，并校验系统声、摄像头、麦克风及旁白压低区间与新时间线同步；
- 直接切换、交叉叠化、淡入黑场的非破坏项目格式、旧项目兼容、邻接片段时长限幅、重叠成片时长和主导源时间映射；
- 两轨交替 AVFoundation 合成与真实 H.264 导出：红/蓝片段叠化中点必须同时含两色，黑场转场中点必须为真实黑帧；
- 重叠转场经过自动运镜/光标/背景管线后仍保留像素混合；系统声、独立麦克风和摄像头侧轨按同一重叠布局同步重生；
- 摄像头/麦克风侧轨短于屏幕轨时保留时间线空隙，后续重排片段不会提前造成音画错位；
- 统一编辑器对分割/变速/移出、效果参数、撤销/重做、已保存/未保存边界的状态测试，以及 1260×780 原生编辑器快照；
- 视频标注开放格式的淡入淡出、越界归一化、旧编辑计划兼容，以及剪切、变速、重排和重复片段下的源时间→成片区间映射与转场权重同步；
- 九类视频标注的画布绘制/选择/移动/四角或箭头端点缩放、颜色/时长/淡入淡出/线宽/效果强度、连续手势单次撤销和时间线区间状态；
- Core Image 模糊/像素化只改变指定区域，矢量标注在真实 H.264 中仅于源时间活动区间烧录，区间外像素保持不变；
- 编辑器摄像头画布拖放/缩放、连续手势合并为单次撤销、真实素材比例、摄像头缩略图和丢帧占位容错；
- 摄像头源时间关键帧在剪切/变速/重排/重复片段中的多点映射，时间线菱形标记、前后跳转、新增/更新/删除与五种过渡选择；
- 可丢弃持久索引对 OCR/转写增量失效、损坏索引自动重建、已删除项目清理、符号链接循环/越界防护及搜索结果稳定性的测试；
- `analysis/insights.json` 的开放格式、0.1 向后兼容、原子写入、Manifest 资产登记、持久索引失效与标题/摘要/标签统一搜索；
- 本地抽取式标题、摘要、标签、要点和录屏章节，以及中日文紧凑文本、应用元数据与时间边界的确定性测试；
- 邮箱、电话、Luhn 有效卡号、18 位证件号和凭据检测；跨两个转写片段的敏感值定位，以及整理 JSON 不包含命中原值的隐私断言；
- 人工标题/摘要/标签覆盖层的归一化、搜索、恢复自动结果与旧版整理文档兼容测试；
- 含换行标签、章节、脱敏提示和人工校正状态的 380×520 原生玻璃整理面板快照；
- 开放转写文档对时间片段归一化、项目包原子写入/读取和标题/OCR/转写统一搜索的往返测试；
- Apple Speech 时间片段到设备端转写文档的映射、缺失音轨安全拒绝和语音识别权限状态映射；
- 125 秒音轨的 50 秒/1 秒重叠分片计划、边界中点单一归属、时间偏移和重复词剔除；
- 合成 CAF 指定区间实际导出为 Speech 可读 M4A，并校验音频轨、时长与非空输出；
- 中文紧凑断句、英文标点间距、人工字幕副本与空文本隐藏的非破坏性测试；
- 字幕源时码归一化、相邻边界限幅、自然标点分割、中英文合并间距、删除、选择、定位及逐操作撤销/重做；
- 字幕源时间跨剪切、片段重排与变速映射到成片时间的同步性测试；
- Core Image 字幕叠加的活动区间像素检查，以及真实 H.264 MP4 中玻璃背景/文字只在字幕时段出现的媒体集成测试；
- 字幕开关、三种样式、三个位置、字号、源时码输入、播放头分割、相邻合并、逐条校对副本及撤销的编辑器状态测试；
- 原画/平衡/轻量导出预设的开放格式往返与旧项目原画兼容；60 fps 合成 H.264 在轻量预设下真实输出约 24 fps，并验证可播放编码与实际样本数；
- 1260×780（Retina 2520×1560）编辑器底部检查器专项快照，人工确认字幕操作与三档导出在 292px 宽度内无挤压、截断或底部遮挡；2026-08-10 最新快照发现时间线标注带因中心对齐偏移越界并遮挡速度标签，现改为轨道内偏移并裁切边界，复拍摘要为 `35a10dd13cc302267d80cfd8c87cf038de8b1f0d699c63dace5b849506dd1224`，速度、转场、字幕和导出区域均无遮挡；
- 合成摄像头视频的独立视频轨校验、空轨拒绝、默认画中画布局及自动编辑计划更新后的布局保留；
- 暂停/继续分片索引的创建、完成、累计时长、物理轨登记与空分片清理；
- 两段 H.264 的无重编码顺序合并、单个恢复分片重建主文件、首段归档后主文件合并且原始首段不变；
- 两段独立 CAF 的 PCM 拼接、采样率/声道与精确总帧数检查；
- 录制控制条真实暂停/继续计时、过渡态禁用、双路音频电平、重录请求在切换状态下的拦截，以及暂停视觉快照；
- 录制控制条项目磁盘容量读取、未知/充足/预警/严重四态、5 GiB/1 GiB 边界、严重状态单次安全停止回调，以及含“丢弃并重录”的 590×98 视觉快照；
- 圆形画中画遮罩、圆角矩形、四角锚点、镜像、边距与阴影的 Core Image 像素检查；
- 合成屏幕/摄像头视频的实际 H.264 画中画导出与目标色块检查；
- 自由归一化定位、边界夹取、字幕冲突避让、点击焦点远离、字幕前摇/回落平滑量与旧 `0.3` 计划兼容读取；
- 真实 H.264 导出中的摄像头关键帧移动，对前后两帧绿色人像像素质心做定量断言；
- 画中画导出后的系统音频轨保留，以及损坏摄像头轨自动退回屏幕预览；
- 画中画视频编码与系统声 passthrough 回填分离，避免 AVAssetWriter 双输入背压死锁；曾复现失败的媒体测试连续 10 次通过；
- 麦克风/摄像头权限状态映射及权限中心动作呈现；
- 语音识别授权回调通过显式非隔离 `@Sendable` 桥接收 TCC 后台队列入口，再切回 MainActor 刷新；`Task.detached` 回归测试覆盖真实 `.ips` 指向的 Swift executor trap；
- OCR 文档序列化、Manifest 资产登记、原始截图不变和错误项目类型拒绝；
- 合成英文文字图片的真实 Apple Vision 识别、文本输出与归一化边界框；
- 截图标注计划对矩形、椭圆、箭头、自由画笔、高亮、自动编号、文字、模糊和像素化九类对象的 Codable 往返和源尺寸约束；
- 矩形、椭圆、箭头、文字的实际像素输出，以及模糊/像素化只影响目标区域；
- 标注保存服务对原图不变、计划文件、渲染预览和 Manifest 资产的完整闭环；
- 标注编辑模型的反向拖动、箭头方向、文字单击、撤销/重做与清空；
- 自由画笔路径采样/简化/命中/移动/缩放、高亮半透明填充与自动编号连续性；
- 带固定页头的合成滚动视口纵向对齐，重复帧去除、页头只保留一次、多段位移累计与最终像素高度检查；
- 长截图的尺寸变化拒绝、内存/像素上限、定时取帧与用户收尾并发、连续两次捕获回调及玻璃控制条快照；
- `raw/scrolling/frame-*.png` 源帧、`events/scrolling-capture.json` 放置/重叠计划与 Manifest 资产的原子归档，重复写入时不破坏已有源帧；
- 共享标注几何层的顶层对象命中、箭头线段命中、画布边界移动、四角缩放、文字字号缩放和箭头端点缩放；
- 编辑器选择模式的顶层对象选择、移动、缩放、换色、删除及逐操作撤销；
- 1080×720 逻辑尺寸下含选中框与控制点的原生编辑器离屏快照，并验证 Retina 2× backing scale；
- 屏迹库对损坏项目的容错扫描、按时间倒序、渲染资产优先和 OCR 读取；
- 标题/OCR 多词搜索、音调符号折叠、截图/录屏类型筛选及数量统计；
- 1020×690 逻辑尺寸下混合截图/录屏屏迹库的原生离屏快照；
- 含截图/录屏双菜单的操作中心快照、截图/录屏选区提示语义，以及显示当前来源的录制控制条快照；
- `.screentrace` 截图/录屏项目生命周期、事件轨、恢复与原始视频校验；
- 自动运镜的点击聚焦、连续点击重定向和边缘约束；
- 光标自适应平滑、坐标约束与 seek-safe 插值；
- 合成测试视频的自动预览导出；
- 背景画布、圆角/阴影、光标与点击反馈的渲染链路；
- 输出视频轨、时长、尺寸和画布像素检查。

## 构建与包检查

以下检查全部通过：

```bash
swift build -c release --product ScreenTrace -Xswiftc -warnings-as-errors
bash Scripts/build-app.sh release
plutil -lint Build/ScreenTrace.app/Contents/Info.plist
codesign --verify --deep --strict Build/ScreenTrace.app
```

生成物：`Build/ScreenTrace.app`。当前为本地 ad-hoc 签名，不能替代发布前的 Developer ID 签名与 Apple 公证。

另已执行：

```bash
bash Scripts/build-release-artifacts.sh
```

结果：生成 `Build/Symbols/ScreenTrace-0.1.0-1.dSYM.zip`，zip 完整性通过，App 与 dSYM 的 arm64 Mach-O UUID 均为 `DD164C85-0106-38DF-8B8D-F63E44323285`，归档 SHA-256 为 `e3450c0849716df05a0e5ae8add7c41ad7cbd9dd9b7fdb701e97a2465ab9ab06`。

另已执行：

```bash
bash Scripts/package-macos-release.sh
cd Build/Releases
shasum -a 256 -c ScreenTrace-0.1.0-1-SHA256SUMS.txt
```

结果：生成 2.1 MiB DMG、2.0 MiB App zip 和 4.0 MiB dSYM zip；DMG 两次 `hdiutil verify` 通过，挂载后 App 的 ad-hoc 签名、Info.plist 和 Applications 链接通过。当次 SHA-256 依次为 DMG `322f86211d2d83ffaf330c46ce742216042d9ed613744548095405c81b7f211f`、App zip `fbeeba9a2d1f0ede722a99b4ff98f37a9c21ff6fe6fe2cdfe3e03861f9111a82`、dSYM zip `e3450c0849716df05a0e5ae8add7c41ad7cbd9dd9b7fdb701e97a2465ab9ab06`，校验和文件反向复核三项均为 `OK`。打包链现另生成机器可读 `release.json`，记录版本、最低系统、Git commit/工作区、Mach-O UUID、签名、Team ID、公证 request ID 和三类制品尺寸/摘要，并把该清单纳入 SHA-256 文件。当前签名明确为 `ad-hoc`、无 TeamIdentifier，未提供公证 profile，不等同于可公开分发的 Beta。

Finder 实际预览 `Build/ScreenTrace.app` 后曾写入 `com.apple.FinderInfo` 与用户标签，旧打包逻辑复现 `resource fork, Finder information, or similar detritus not allowed` 并拒绝重签名。构建脚本改为先校验固定生成路径再完整重建 App；同一台机器重新执行 Release 构建、严格验签与 DMG 打包全部通过，不再继承 Finder 污染或旧签名资源。

独立发布验收器另从 `release.json` 出发，不复用打包过程中的临时状态：反查四项 SHA、三类制品尺寸、Git commit/clean、版本、最低系统、App/dSYM UUID、图标、App zip 布局、只读 DMG、Applications 链接和严格签名结构。`--development` 允许当前 ad-hoc/未公证包完成结构验证；公开模式必须在同一结构检查后继续满足许可证、Developer ID、Team ID、App/DMG 公证 Accepted、Staple 与 Gatekeeper，并在退出码 65 前汇总全部资格阻断项，不再只报告首个缺失条件。

正式 App 图标主图固定为 1024×1024 不透明 RGB PNG；已人工复核 128、32 和 16 像素辨识度。构建脚本从唯一主图生成十档标准 iconset 与 `AppIcon.icns`，公开源码审计只允许该固定路径与 SHA-256 的 PNG。Release App 仍需复核 Info.plist 引用、资源非空和签名后完整性。

离线符号化脚本已通过两类实测：本机旧 `.ips` 与当前 dSYM UUID 不一致时以状态 67 拒绝；使用当前 UUID/base/image offset 构造的合成 `.ips` 成功输出 `CaptionCuePlanner.swift:245` 的 Swift 符号化帧。

## 已完成的本机 UI 冒烟测试

- 操作中心可启动并显示原生迹光玻璃界面；
- 辅助功能树能识别主要按钮；
- `Esc` 可关闭操作中心；
- 缺少屏幕录制权限时能进入权限说明流程；
- 录制控制条能获得焦点、计时、切换控制状态并关闭；
- 修复了录制控制条没有成为 key window 的问题；
- 解锁后确认操作中心的迹光玻璃布局、辅助功能树和截图/录屏双菜单均可用；2026-08-10 再次从真实 App Bundle 展开菜单并读取辅助功能树，截图菜单完整呈现区域、窗口、多窗口和当前屏幕，录屏菜单完整呈现三种来源、系统声、独立麦克风、独立摄像头、帧率、自动转写整理和语言；为避免触发未授权捕获，本轮只验菜单路由，多窗口真实捕获仍待屏幕录制授权后实点；
- 新增的录制帧率菜单已在真实 App Bundle 中验收，可正常呈现“30 FPS · 省空间”和“60 FPS · 更流畅”并安全关闭；
- 新增的转写语言菜单已在真实 App Bundle 中验收，可正常呈现跟随系统、简中、繁中、英语、日语和韩语六档并安全关闭；
- 快捷键设置已在真实 App Bundle 中验收：成功把快速截图录入为 `Shift + Command + C`、重启后保持，并通过“恢复默认”回到 `Fn + Control`；修复了录入完成后隐藏捕获视图残留 first responder、吞掉后续按键的问题；
- 屏迹库空状态、搜索输入和截图/录屏类型筛选可操作，计数与筛选状态同步；
- 权限中心能正确显示屏幕与系统音频、麦克风、摄像头、本地语音识别和辅助功能状态；2026-08-10 实际状态为麦克风、摄像头已允许，屏幕与系统音频、辅助功能未允许，本地语音识别尚未请求；
- 诊断与支持区已在真实 App Bundle 中滚动到底验收：隐私说明完整、“复制诊断摘要”可点击，并显示绿色“诊断摘要已复制”反馈；启动日志已实际写入 Application Support 下的 JSONL；
- Release App 首次启动实际生成仅含版本/构建/会话 UUID/时间的活动标记；使用 `SIGKILL` 模拟强退后重启，JSONL 实际写入 `app.previous_session_unclean` 警告且不含崩溃正文；
- 最终 Release App 实际识别到本机 1 份 ScreenTrace `.ips`，JSONL 仅写入 `crash_reports.available` 及 `count=1`；诊断摘要的脱敏指纹格式已由合成 `.ips` 覆盖，原始调用栈不写入日志；
- 未授权截图会显示“需要屏幕录制权限”说明，并提供稍后处理和打开系统设置两条安全路径。
- 当前 Release App 的操作中心、截图/录屏菜单、设置与权限、屏迹库已通过真实辅助功能树复核；截图/录屏模式菜单、帧率、转写语言以及新增的图标按钮名称和提示均可被读取。
- 屏迹库的搜索、刷新、Finder、关闭和类型筛选均正确暴露；Escape 实际关闭窗口。操作中心关闭后从 Finder 再次打开 App 原先只激活不显示，现通过 `applicationShouldHandleReopen` 修复并在驻留进程上完成“关闭 → Finder 再打开 → 操作中心出现”的真机回归。
- 麦克风从“尚未请求”经真实系统授权回调进入“已允许”，摄像头也已由权限中心真实状态确认“已允许”；本地语音识别仍待用户授权后确认，不以界面按钮点击代替最终权限状态。
- 2026-08-10 用旧版黄金夹具和本机生成的 1280×720、30 fps、8 秒无隐私 H.264 建立临时项目，真实 App 从屏迹库读取后首次打开编辑器触发 `SIGABRT`；最新 `.ips` 的匹配 UUID 故障栈指向 `_AVKit_SwiftUI` 元数据初始化。预览改为原生 `AVPlayerView` 桥接并隐藏其内部无意义辅助功能子树，新增“已有 `AVPlayerItem` 时必须挂载原生播放器”回归测试；重建 App 后真实打开、播放至 8 秒结束、字幕开关、校对展开、平衡/轻量预设、撤销和恢复原方案均通过，用户项目未写回。干净提交 `64d8381` 生成的 Release App/dSYM UUID 为 `432DD58B-DF24-3CAB-AD53-67B99096F7E4`，App zip、dSYM 和 DMG 经独立开发验收器通过，同一 Release App 再次真实打开/关闭编辑器且没有新增 ScreenTrace `.ips`。临时项目已移出 `~/Pictures/ScreenTrace/` 到忽略的 `Build/E2E/`，刷新屏迹库从 1 条恢复为 0 条并清理可丢弃索引。
- 另一份真实故障报告 `ScreenTrace-2026-08-10-015915.ips` 为主线程 `SIGBUS`，App 自身栈落在全局快捷键事件监听闭包，顶部为 `swift_task_isMainExecutorImpl`；根因是 AppKit 全局回调在非主执行上下文调用了从 `@MainActor` 词法环境生成的闭包，并把 `NSEvent` 带入新任务。快捷键监听已改为非隔离 `@Sendable` 纯值桥接和监听代际校验，Carbon 排队回调也在停止后拒绝；相同模式的录制事件采集同步修复。两条后台生命周期回归、256 项全量测试和 Release warnings-as-errors 构建全部通过。
- 安装版已用真实当前显示器完成一次截图，Quick Access 报告 `2560 × 1664` 并保存项目；随后完成 26.16 秒屏幕+系统音频+麦克风+摄像头录制，实际暂停/继续形成两段并保留指针、点击、快捷键与窗口事件轨。合并后的屏幕视频为 `2560 × 1664`、约 57.2 fps，系统声音轨与屏幕时长一致，麦克风 CAF 和 `1920 × 1080` 摄像头 MOV 均独立存在；自动预览、圆形人像、自动运镜、光标/点击效果、背景、圆角、混音和三档导出控件已在统一编辑器中复核。
- 麦克风真实采样与本地语音识别授权回调各发现并修复一处 Swift 并发执行器崩溃；最新安装版使用带音频输入/摄像头 entitlement 的本地稳定证书签名，构建与 `/Applications/ScreenTrace.app` 可执行文件摘要一致并通过 `codesign --verify --deep --strict`。该证书无 Developer ID Team ID，只解决开发阶段构建身份稳定，不满足公开发布资格。
- 两次自动转写均由 Apple Speech 返回 `kAFAssistantErrorDomain 1110`。实际麦克风轨峰值约 `-27.6 dBFS`、整体 RMS 约 `-49 dBFS`，只证明录到了安静房间/键盘声，不能据此判定真人语音转写或字幕通过；仍需用户对麦克风清晰朗读后重测。
- KlipKap 动画页面暴露出持续动画可能被误判为滚动、导致输出高度异常增长的问题。长截图会话现要求变化后的视口连续两次稳定后才提交给拼接器；连续运动帧只提示“等待稳定”，静态滚动停稳后正常追加。新增两条会话级回归后，全量 269 项测试和 warnings-as-errors Release 构建通过，修复提交为 `4a97d33`；真实 Safari 静态长页捕获因 Mac 自动锁屏在中段暂停，解锁后需从安装版重新验收。
- 五项系统权限现已在稳定签名安装版中全部显示“已允许”：屏幕与系统音频、麦克风、摄像头、本地语音识别和辅助功能。删除旧辅助功能条目、重新添加 `/Applications/ScreenTrace.app` 并重启后，内置键盘物理 `Fn + Control` 已真实进入区域截图遮罩，`Fn + 空格` 已从无窗口状态重新呼出操作中心；两条默认快捷键不再仅由代码或合成事件间接证明。
- 为避免把安静麦克风误判为语音识别故障，另用系统中文语音生成 12.42 秒本地 CAF，并放入隔离的 E2E 录屏副本。真实安装版通过 Apple 设备端 Speech 生成 23 个源时码片段，写入 `analysis/transcript.json`，自动开启玻璃字幕、生成 4 条可校对字幕、完成本地整理并重新生成带字幕预览；编辑器中逐条时码、合并/删除入口、自动运镜、平滑光标、混音、摄像头和三档导出状态均可读取。首词“屏迹”被设备端模型识别为“经济”，属于识别准确率而非链路失败；真人口音/环境准确率留作体验验收，不再阻塞自动回归。
- 安装版另完成一次真实区域 OCR：在当前屏幕拖出 1504×738 像素选区，Quick Access 显示“文字已复制 · 8 段”，项目以区域来源坐标、`apple-vision` 引擎、`zh-Hans`/`en-US` 语言和 8 个归一化文本块写入 `analysis/ocr.json`，随后生成 `analysis/insights.json`。识别结果包含中英文混排且可由屏迹库读取；该无隐私测试项目已移到忽略的 `Build/E2E/ArchivedValidation/`，刷新后正式库从 23 条恢复为 22 条。
- 同一安装版从操作中心把最近截图真实贴到桌面，浮层辅助功能树暴露复制、缩小、放大、透明度、锁定和关闭六个控件。复制动作可执行；窗口从 520×256 缩小到 434×213，再放大恢复 520×256；透明度从 100% 切到 80%；锁定后按钮提示变为“解锁位置”，再次点击恢复；关闭后浮层消失并回到操作中心。右键菜单中的 Finder 定位及九类截图标注尚未纳入本次通过范围。
- 提交 `6233552` 时的 269 项测试、warnings-as-errors Release 构建和开发发布包均通过；该包经完整重建、DMG 两次校验、App/dSYM UUID 对齐、App zip/dSYM zip 完整性、挂载后严格验签和独立 `--development` 验收全部通过。Mach-O UUID 为 `8944BAF7-A926-335F-9CB5-EC46102618E1`，DMG、App zip、dSYM zip、`release.json` 的 SHA-256 分别为 `d44afbe1c1644725ec08434d0f5a4cc9d964ae79c7fdbae61c2ceb1058de4cb9`、`eacae9d8cf5f900aee7aea60bd10a590854756ffd73b3b1c880c7cd0126b06d0`、`cee7a20b4f8f3fbbc044d1a1a2303be45549833cd79bf69db0b472fd8f28c1be`、`b1bfb2a2005e41e00342c1d5e7b2f742ee331fd88a5dbfc210a0fc3f42b64178`。
- `/Applications/ScreenTrace.app` 被 Finder 写入根级 `com.apple.FinderInfo` 与用户标签后，严格验签会按预期拒绝污染包；只移除这两项 Finder 元数据后再次显示 `valid on disk` 与 `satisfies its Designated Requirement`。安装版仍由 `PathShot Local Code Signing` 签名、启用 hardened runtime、无 Team ID；公开模式验收一次汇总 12 个阻断项，实质类别仍是根许可证、Developer ID/Team ID、App 与 DMG 公证/Staple/Gatekeeper，以及 DMG 发布签名。
- 安装版已从 Quick Access 真实进入 1080×720 截图标注编辑器；九类工具、六种颜色、选择、背景画布、导出、完成、清空、撤销/重做及“原图始终保留”说明均正确暴露给辅助功能树，取消后没有写入编辑计划。当前 Computer Use 服务只能切换工具和移动指针，不能把按下/拖动事件送进 SwiftUI 画布，因此对象数始终为 0，不能把九类真机手势冒充通过；三份无隐私临时截图已移入忽略的 `Build/E2E/ArchivedValidation/AnnotationValidation-20260810/`，重启后正式资料库没有遗留新增项目。
- 同一轮长时间自动化会话中曾观察到一次安装版约 99% CPU 且辅助功能读取超时；3 秒采样显示主线程持续处于 SwiftUI 布局/AttributeGraph 更新。正常终止并干净重启后，连续复测“截图 → 打开标注 → 画布单击 → 取消”保持约 0.5–0.7% CPU，无法复现，也没有崩溃报告或可归因到业务代码的栈帧，因此本轮不做猜测性修复，保留为九类标注人工验收时的性能观察项。
- 屏迹库组合路径已用归档的真实 OCR 截图和合成语音录屏副本完成安装版回归：启动扫描从 22 条正确变为 24 条（截图 13、录屏 11）；转写词“经济正在测试”在全部/录屏筛选下只命中目标录屏，OCR 词“Vision识别”在录屏筛选为 0、切换截图后只命中目标截图。录屏可从卡片打开统一编辑器；截图复制动作可执行，打开动作由 Preview 读取目标项目的 `raw/screenshot.png`，标注入口可打开并无改动取消；Finder 动作选中了目标项目的 `previews/auto.mp4`。另在隔离副本中把标题/摘要/标签校正为“E2E 人工校正验收”/验收摘要/`#验收 #本地`，卡片与搜索立即命中校正值；点击“恢复自动”后校正词命中数变为 0，原自动标题、摘要和标签恢复，原转写搜索继续命中。两个测试副本已移到忽略的 `Build/E2E/ArchivedValidation/LibraryFlowCopies-20260810/`，重启后资料库恢复 22 条（截图 12、录屏 10）。
- 安装版“丢弃并重录”已完成真机回归：首次当前显示器 60 FPS 录制同时生成屏幕、系统音频、独立麦克风、摄像头与事件轨；二次确认后先安全停止旧项目并移入废纸篓，再按相同来源自动开始新录制，麦克风和摄像头轨保持启用。第二段停止后正常生成 `previews/auto.mp4`；旧项目可由 Finder“放回原处”恢复，证明删除路径可逆。两段项目已移到忽略的 `Build/E2E/ArchivedValidation/RerecordValidation-20260810/`，不污染正式资料库。
- 安装版完成 616.6 秒当前显示器 60 FPS 连续录制：屏幕 616.64 秒（2560×1664）、摄像头 616.87 秒（1920×1080）、麦克风 618.4 秒，系统音频、事件轨、分片索引和编辑计划完整；两阶段自然模式生成后 `auto.mp4` 为 616.67 秒（2560×1664），项目从 `processing` 进入 `ready`，可由屏迹库打开 10:16 统一编辑器。约 3.36 GB 的无隐私项目已移到忽略的 `Build/E2E/ArchivedValidation/LongRecordingValidation-20260810/`，正式资料库恢复 22 条。
- 真实录制 13 秒后对安装版发送 `SIGKILL`，磁盘上的项目在退出瞬间仍为 `capturing`，屏幕、摄像头和麦克风文件均非空。旧实现重启后只标记 `interrupted`，编辑入口不能恢复时长，因而补上媒体级恢复：逐个校验可播放视频分片、恢复分片时长、归档/合并暂停前后分片、重建编辑计划并继续生成预览；已有 `processing` 项目也会在下次启动续做，或直接认领已原子落盘的 `auto.mp4`。稳定签名安装版用同一真实中断项目复测后恢复出 21.97 秒源时长并进入 `ready`，预览 20.5 MB；样本已移到忽略的 `Build/E2E/ArchivedValidation/CrashRecoveryValidation-20260810/`，正式目录物理计数恢复 22。新增单段/多段与处理中续跑覆盖后全量 271 项测试、0 失败，warnings-as-errors Release 构建通过。
- 操作中心 688×430 与统一编辑器 1260×780 现各增加一条 `accessibilityHighContrastDarkAqua` 深色高对比度渲染回归，并保留原有常规外观快照；两种主要窗口都能完成布局和非空像素输出。全量基线上升到 273 项、0 失败。降低透明度、减少动态效果和真实 VoiceOver 仍需在解锁系统设置下完成，不用这两张静态快照冒充通过。
- 安装版在 Safari 本地静态长页上完成独立窗口录制：Manifest 记录 `mode=window`、Safari 窗口标题/ID、Retina 2232×1452、60 FPS 与屏幕/系统声/麦克风/摄像头四轨，17.23 秒预览进入 `ready`。同轮区域录制使用 1574×1070、60 FPS，连续两次暂停/继续形成 16.153、12.717、8.450 秒三段，时间线偏移连续，屏幕/麦克风/摄像头分片和 37.33 秒预览均完整。两个项目与后续长截图样本已移到 `Build/E2E/ArchivedValidation/SafariCaptureValidation-20260810/`，正式目录恢复 22 条。
- Safari 真实长截图首次 9 帧捕获生成 2016×5380，第二次 14 帧生成 1736×4604；源帧、计划、OCR 和项目归档链路都可用，但目检发现纯色卡片页面把约 750 px 的滚动误判成 61 px，最终图出现覆盖、跳过 03–07/12–13 等内容。根因是 96 px 灰度签名和过大的上下内缩让稀疏文字/边框权重不足，且无法搜索约 78% 视口的大步滚动。签名现提升到 256 px、以文字/边缘梯度加权，允许保留至少约 18% 重叠；新增大面积纯色网页卡片回归。将原 14 张真实 Safari 源帧交给修复后的同一拼接器离线重放，得到 1736×10112 的完整 01–15 连续长图，固定页头只保留一份，无黑线、覆盖或跳段，证据为 `Build/E2E/safari-scroll-fixed.png`。全量基线上升到 274 项、0 失败，Release warnings-as-errors 构建通过；修复版已替换安装包，因随后自动锁屏未把“安装版重新捕获并落盘”冒充已复测。
- 项目存储异常现在统一给出可执行的恢复建议：无写入权限/只读卷提示复制整个 `.screentrace` 到可写磁盘或恢复权限，磁盘满明确说明已写入的原始媒体和分片继续保留，项目缺失提示从备份恢复完整项目包；截图、长截图、录屏、截图标注和视频编辑失败入口均复用同一映射，非存储错误保持原系统说明。真实 POSIX 只读 `analysis/` 回归证明 OCR 写入失败后原图与 Manifest 字节不变且项目仍可进入屏迹库；不可写根目录回归证明不会遗留半成品项目包。另从旧版原始偏好键直接加载快捷键、四项媒体/转写设置和帧率，覆盖升级不重置设置。
- 操作中心按钮弹簧与录制音频电平动画现读取 `accessibilityReduceMotion`：开启减少动态效果时保留颜色/数值反馈但取消缩放和插值动画；`TraceGlassPanel` 继续在降低透明度时改用 97% 不透明窗口背景。操作中心 688×430 和统一编辑器 1260×780 新增降低透明度+减少动态效果组合渲染回归，并用策略断言动画为 `nil`。加上存储/升级回归后全量基线为 282 项、0 失败；真实系统设置与 VoiceOver 仍保留真机门槛。
- 发布工程新增受约束的 `alpha`/`beta`/`stable` 发布级别，当前候选固定为 `0.1.0 (1)`、`beta`、最低 macOS 15.2。打包脚本会从机器清单和四项核心摘要自动生成发布说明，固定包含能力、权限、数据位置、开放项目兼容性与已知限制，再把发布说明自身加入第五条 SHA-256 校验；独立校验器拒绝缺失、被篡改、身份字段漂移、章节缺失或摘要不一致的发布说明。实改发布级别但保留旧摘要的反向夹具已被拒绝，摘要格式错误或内容不匹配均统一返回发布完整性状态 65。

当前开发安装版已完成五项权限、内置键盘两条默认 `Fn` 快捷键、当前显示器/独立窗口/区域四轨录屏、重复暂停继续、同来源重录、10 分钟连续录制、强退恢复、本机转写、字幕、整理、编辑器和自动预览的纵向链路。Safari 静态长页已完成真实源帧捕获、故障复现和修复后完整重放；安装版修复后再次落盘、Chrome/普通 App/KlipKap 动画页、更广的键盘/多屏/缩放、1 小时录制、设备/磁盘异常和 VoiceOver 矩阵仍需专项真机验收。

## 仍需人工/真机完成

1. 使用 Magic Keyboard 与第三方键盘补齐默认、自定义及无 `Fn` 备用快捷键矩阵；内置键盘默认 `Fn + Control` 与 `Fn + 空格` 已通过。
2. 验证单屏、多屏、Retina、不同缩放比例和屏幕排列下的截图坐标，并覆盖跨屏多窗口、混合缩放与重叠层级。
3. 补齐设备拔出、系统流中断和磁盘异常；当前显示器、独立窗口、区域四轨录屏与区域两次暂停继续已通过。
4. 用修复后的安装版再次完成 Safari 静态页落盘，并补齐 Chrome/普通 App 的前台区域选择 → 手动滚动 → 自动取帧/去重/拼接 → 完成复制 → 源帧/计划归档兼容矩阵；复测 KlipKap 持续动画页面不会异常增高。Safari 真实 14 帧的修复后离线重放已通过。
5. 补齐贴图右键菜单的 Finder 定位与整体悬停手感，再完成九类截图标注的真机手势与再次编辑；Quick Access → 贴图 → 复制/缩放/透明度/锁定/关闭已通过。
6. 补齐编辑器入/出点、分割、变速、移出、摄像头拖放/缩放/避让/关键帧、九类视频标注和四轨同步重生的真人视觉验收；设备端转写、字幕分句/时码、整理和预览生成已通过合成语音真机链路。
7. 进行 1 小时录制，以及磁盘不足、设备拔出和系统流中断恢复测试；10 分钟连续录制与强退自动恢复已通过。
8. 在真实系统设置中复核浅色/深色、降低透明度、增强对比度和减少动态效果；深色高对比度及降低透明度+减少动态效果的主要窗口自动渲染已通过。
9. 使用 VoiceOver 逐项验证动态朗读、图标按钮名称、焦点顺序和键盘操作；无辅助功能客户端的 XCTest 进程不能替代此项。

## 当前不应宣称已经完成的能力

- 摄像头在预览画布上的直接拖动/缩放、自动避让和关键帧，以及九类视频标注的解锁后真机手势与成片视觉验收；
- 多窗口截图的跨屏/不同缩放真机回归，以及长截图在真实浏览器/普通 App 中的兼容矩阵；
- 可选生成式模型、语义聚类与批量整理；
- 发布级签名、公证与 Windows 版本。
