[package]
name = "lens-format"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Portable .lens project schema and path-safe document reader"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

